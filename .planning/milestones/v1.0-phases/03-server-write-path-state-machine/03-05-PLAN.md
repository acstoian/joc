---
phase: 03-server-write-path-state-machine
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-04"]
files_modified:
  - src/app/api/game/state/route.ts
autonomous: true
requirements: [SCOR-02, HOST-04]
must_haves:
  truths:
    - "GET /api/game/state returns correctOption ONLY when phase === 'revealed'; it is null/omitted in every earlier phase"
    - "GET /api/game/state returns the A/B distribution when phase is 'locked' or 'revealed', else null"
    - "GET /api/game/state returns a leaderboard ranked by correct_count descending when phase is not 'lobby'"
    - "The correctOption read uses the base questions table via adminClient (never questions_public) and only inside the revealed gate"
  artifacts:
    - path: "src/app/api/game/state/route.ts"
      provides: "Extended GET /api/game/state with phase-gated correctOption + distribution + leaderboard (D-02)"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/game/state/route.ts"
      to: "questions.correct_option (base table, revealed-only)"
      via: "adminClient.from(\"questions\").select(\"correct_option\") gated on phase === 'revealed'"
      pattern: "correct_option"
    - from: "src/app/api/game/state/route.ts"
      to: "scores + players (leaderboard)"
      via: "adminClient scores join ordered by correct_count desc"
      pattern: "correct_count"
---

<objective>
Complete the read side of the slice: extend the existing `GET /api/game/state` (D-02 — one endpoint serves everything, not new endpoints) to populate the three fields Plan 01 added to `GameStateSnapshot` — `correctOption` (post-reveal only), the A/B `distribution` (locked/revealed only), and the `leaderboard` (ranked by `correct_count` desc, phase != lobby). After this plan, every client's subscribe-then-fetch resync returns the authoritative reveal/distribution/leaderboard data, replacing the Phase 2 `correctOption: null` stub — closing SC5 ("the leaderboard … ranks players by correct_count descending") via D-01's re-fetch interpretation.

Purpose: This is the read endpoint that `useGameSync` calls on every (re)connect and after every broadcast signal — the signals are pure (D-06), so this fetch is where the data actually arrives (D-01). The hardest constraint is answer secrecy: `correctOption` must be read from the BASE `questions` table (via adminClient) and ONLY when `phase === 'revealed'` (Pitfall 3 — reading it earlier, or via the wrong table, leaks the answer pre-reveal). It owns only `state/route.ts`. It depends on Plan 01 (the extended type) and Plan 04 (reveal populates `correct_option` + scores) so the end-to-end verification has real data to assert — it runs in Wave 3.

Output: `src/app/api/game/state/route.ts` (modified — extends the existing Phase 2 handler).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-write-path-state-machine/03-CONTEXT.md
@.planning/phases/03-server-write-path-state-machine/03-RESEARCH.md
@.planning/phases/03-server-write-path-state-machine/03-PATTERNS.md
@.planning/phases/03-server-write-path-state-machine/03-VALIDATION.md
@.planning/phases/03-server-write-path-state-machine/03-01-SUMMARY.md
@.planning/phases/03-server-write-path-state-machine/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GET /api/game/state with phase-gated correctOption, distribution, and leaderboard</name>
  <files>src/app/api/game/state/route.ts</files>
  <read_first>
    - src/app/api/game/state/route.ts (the file being MODIFIED — the full existing handler: keep the `UUID_REGEX`/`isValidUuid` validation, the games fetch, the `questions_public` current-question read, and the `maybeSingle()` myAnswer fetch UNCHANGED; the only change is replacing the `correctOption` stub at line 121 and adding distribution + leaderboard to the returned snapshot)
    - src/hooks/useGameSync.ts (Plan 01 — the EXTENDED `GameStateSnapshot` type with `distribution` + `leaderboard`; the returned snapshot object must satisfy this exact shape or the build fails)
    - .planning/phases/03-server-write-path-state-machine/03-PATTERNS.md (section "src/app/api/game/state/route.ts — MODIFY" — the correctOption block, distribution block, leaderboard block, and the stub-replacement note; "Existing pattern to preserve" list)
    - .planning/phases/03-server-write-path-state-machine/03-RESEARCH.md (Pattern 5 phase-gated reads; Pitfall 3 leaking correct_option pre-reveal; A4 leaderboard join shape; D-01 re-fetch / D-02 single endpoint)
    - .planning/phases/03-server-write-path-state-machine/03-CONTEXT.md (D-01 leaderboard-by-re-fetch; D-02 extend the existing endpoint)
    - src/types/database.ts (Row shapes for questions, answers.choice, scores.correct_count, players.display_name, players.game_id — for typed queries)
  </read_first>
  <action>
    Modify `src/app/api/game/state/route.ts` (D-02 — extend, do not create new endpoints). PRESERVE everything before the snapshot construction: the input validation, the games fetch, the `questions_public` current-question read (NEVER switch the question-text read to the base `questions` table — the view omits `correct_option` for secrecy), and the `maybeSingle()` myAnswer fetch. Add three phase-gated blocks computing the new fields, then include them in the returned `snapshot` so it satisfies the extended `GameStateSnapshot`. (1) correctOption block — REPLACE the `correctOption: null` stub (line ~121): default `let correctOption: "A" | "B" | null = null`; ONLY when `game.phase === "revealed" && game.current_question_id`, read `adminClient.from("questions").select("correct_option").eq("id", game.current_question_id).single()` — the BASE `questions` table here is correct and necessary (the view omits the column) but is fenced behind the `phase === "revealed"` gate so the answer is never exposed earlier (Pitfall 3 — this is the ONE place in the read path that touches base `questions`, and only post-reveal). Set `correctOption = (q?.correct_option as "A"|"B") ?? null`. (2) distribution block — default `let distribution: { A: number; B: number } | null = null`; only when `(game.phase === "locked" || game.phase === "revealed") && game.current_question_id`, read `adminClient.from("answers").select("choice").eq("question_id", game.current_question_id)`, then `distribution = { A: rows.filter(a=>a.choice==="A").length, B: rows.filter(a=>a.choice==="B").length }` (per-question counts; ≤100 players, client-side count is fine — A3). (3) leaderboard block — default `let leaderboard: { name: string; score: number }[] = []`; only when `game.phase !== "lobby"`, read `adminClient.from("scores").select("correct_count, players!inner(display_name)").eq("players.game_id", gameId).order("correct_count", { ascending: false }).limit(20)`, then map to `{ name: players.display_name, score: correct_count }` (ranked desc — SCOR-02). Add all three to the returned snapshot: `{ phase, currentQuestionId, currentQuestion, myAnswer, correctOption, distribution, leaderboard }`. Update the file header comment to note Phase 3 now populates correctOption (revealed), distribution (locked/revealed), leaderboard (non-lobby), replacing the Phase 2 stub. If the leaderboard join shape errors at build (A4 fallback), split into two queries (scores then players) rather than abandoning the field.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Against the seed game with reveal applied (run a reveal via Plan 04 first so scores + correct_option exist): (1) with phase `'question'`, `curl "localhost:3000/api/game/state?gameId=<seed>&playerId=<p>"` returns `correctOption: null` and `distribution: null` (answer NOT leaked pre-reveal — Pitfall 3). (2) with phase `'locked'`, the same call returns `distribution: { A, B }` matching the recorded answers and `correctOption: null`. (3) with phase `'revealed'`, it returns `correctOption` == the revealed choice AND a `leaderboard` array ordered by score descending (top scorer first — SCOR-02). (4) confirm the response still includes the unchanged `phase`, `currentQuestion` (from questions_public), and `myAnswer` fields.</human-check>
  </verify>
  <acceptance_criteria>
    - `GET /api/game/state` returns `correctOption: null` whenever `phase !== "revealed"` and the actual `"A"|"B"` only when `phase === "revealed"` (Pitfall 3 — no pre-reveal leak).
    - The base-`questions` `correct_option` read is fenced inside the `phase === "revealed"` branch; `grep -c "questions_public" src/app/api/game/state/route.ts` ≥ 1 (the question-text read still uses the view) and the only `from("questions")` (base) read is the correctOption branch.
    - `distribution` is `{ A, B }` when phase is `'locked'` or `'revealed'`, else `null`; the counts match the `answers` rows for `current_question_id`.
    - `leaderboard` is ranked by `correct_count` DESC when phase != 'lobby', else `[]` — `grep -c "ascending: false" src/app/api/game/state/route.ts` ≥ 1 (SCOR-02).
    - The returned snapshot includes all seven fields and satisfies the extended `GameStateSnapshot` (build type-checks against Plan 01's type).
    - Existing Phase 2 behavior preserved: gameId 400 validation, games 404, questions_public question read, maybeSingle myAnswer — unchanged.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>`GET /api/game/state` returns phase-gated `correctOption` (revealed only, from base questions behind the gate), `distribution` (locked/revealed), and `leaderboard` (correct_count desc, non-lobby), satisfies the extended `GameStateSnapshot`, and preserves all Phase 2 reads; build passes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan completes the authoritative read endpoint consumed by `useGameSync` on every (re)connect and after every broadcast signal:

| Symbol / File | Kind | Provides |
|---|---|---|
| `src/app/api/game/state/route.ts` → `GET` (MODIFIED) | route handler | Extended `GET /api/game/state?gameId=&playerId=` now returns the full `GameStateSnapshot`: phase, currentQuestion (questions_public), myAnswer, plus newly populated `correctOption` (revealed only, from base `questions` behind the gate), `distribution: { A, B }` (locked/revealed), and `leaderboard: { name, score }[]` ranked by `correct_count` desc (D-01/D-02, SCOR-02, HOST-04 read side) |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client (guest/host/TV) → `GET /api/game/state` | No auth session; `gameId`/`playerId` are attacker-controllable query params; clients must not be able to read the correct answer before reveal |
| server route → Supabase (service_role via adminClient) | Privileged read surface that can see `questions.correct_option`; must gate it behind the revealed phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Information Disclosure | `correct_option` leaking pre-reveal via the state read | mitigate | `correctOption` defaults null; the base-`questions` read is fenced inside `if (phase === "revealed")`; the question-text read stays on the `questions_public` view which omits the column (Pitfall 3, ASVS V4) |
| T-03-18 | Information Disclosure | distribution/leaderboard exposing data in the wrong phase | accept | Distribution (counts only, no identities) gated to locked/revealed; leaderboard (public name + score) gated to non-lobby — both are intended-public aggregates with no secret content |
| T-03-19 | Input Validation | malformed gameId/playerId | mitigate | Existing `isValidUuid` validation preserved → 400 on malformed gameId; playerId validated only when present (ASVS V5) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit: zero new deps) |
</threat_model>

<verification>
- `npm run build` green (snapshot satisfies the extended `GameStateSnapshot`); `npm run lint` green.
- `grep -c "questions_public" src/app/api/game/state/route.ts` ≥ 1 (question text still read via the secrecy view).
- The only base `from("questions")` read is inside the `phase === "revealed"` branch (manual review — Pitfall 3).
- Manual: correctOption null pre-reveal / set post-reveal; distribution present in locked/revealed; leaderboard ordered desc (SCOR-02).
</verification>

<success_criteria>
- `correctOption` is exposed ONLY at `phase === 'revealed'`, read from base `questions` behind the gate, never via `questions_public` (HOST-04 read side, Pitfall 3).
- `distribution` populated in locked/revealed; `leaderboard` ranked by correct_count desc when phase != lobby (SCOR-02, D-01).
- The extended snapshot satisfies the Plan 01 `GameStateSnapshot` type; all Phase 2 reads preserved.
- Build + lint green; no pre-reveal answer leak.
</success_criteria>

<output>
Create `.planning/phases/03-server-write-path-state-machine/03-05-SUMMARY.md` when done.
</output>
