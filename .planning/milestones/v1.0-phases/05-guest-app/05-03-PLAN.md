---
phase: 05-guest-app
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/guest/WinnerScreen.tsx
autonomous: true
requirements: [PLAY-06, PLAY-07]

must_haves:
  truths:
    - "When phase === 'ended' the guest sees a winner screen with the #1 player featured prominently (PLAY-07)"
    - "The full final leaderboard is shown ranked by score (PLAY-06, PLAY-07)"
    - "A canvas-confetti burst fires exactly once when the winner screen mounts (PLAY-07)"
    - "Between rounds the leaderboard updates live below the reveal (PLAY-06 — delivered via LeaderboardPanel already wired in RevealScreen)"
  artifacts:
    - path: "src/components/guest/WinnerScreen.tsx"
      provides: "Game-end screen: #1 featured + full leaderboard + one-shot confetti"
      exports: ["WinnerScreen"]
  key_links:
    - from: "src/components/guest/WinnerScreen.tsx"
      to: "canvas-confetti"
      via: "dynamic import in useEffect guarded by useRef(false)"
      pattern: "import\\(\"canvas-confetti\"\\)"
    - from: "src/components/guest/WinnerScreen.tsx"
      to: "src/components/guest/LeaderboardPanel.tsx"
      via: "renders the full final leaderboard"
      pattern: "LeaderboardPanel"
---

<objective>
Deliver the final vertical slice: the game-end / winner screen. When the host ends the game, the guest sees a celebratory winner screen featuring the #1 player, the full final leaderboard, and a one-time confetti burst — completing the guest journey from join to winner.

Purpose: This replaces the WinnerScreen placeholder created by 05-01 with the full implementation, closing out the guest journey. PLAY-06 (live leaderboard between rounds) is already delivered by the LeaderboardPanel wired into RevealScreen (05-02); this plan adds the final-state leaderboard and the winner treatment. The confetti must fire exactly once.

Decision coverage (CONTEXT.md): D-08 (leaderboard updates live between rounds — final-state full leaderboard), D-09 (game-end/winner screen: full leaderboard with #1 featured in gold/champagne + canvas-confetti burst fired once on entering the ended phase).

Output: Full `WinnerScreen.tsx` (PLAY-07, and the game-end leaderboard portion of PLAY-06) wired into the phase switch already present in `page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-guest-app/05-CONTEXT.md
@.planning/phases/05-guest-app/05-RESEARCH.md
@.planning/phases/05-guest-app/05-UI-SPEC.md
@.planning/phases/05-guest-app/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 05-03)

New files created by this plan (exclude from drift verification):
- `src/components/guest/WinnerScreen.tsx` — exports `WinnerScreen` (replaces the 05-01 placeholder)

This file is imported by the phase switch in `src/app/page.tsx` (created in 05-01); its export name (`WinnerScreen`) must match the import site there.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WinnerScreen — #1 featured + full leaderboard + one-shot confetti</name>
  <files>src/components/guest/WinnerScreen.tsx</files>
  <read_first>
    - src/app/page.tsx (the call site `<WinnerScreen ... />` — match the prop signature exactly; owned by 05-01, do NOT modify)
    - .planning/phases/05-guest-app/05-01-SUMMARY.md (WinnerScreen prop contract; confirm LeaderboardPanel export name/props from 05-01)
    - .planning/phases/05-guest-app/05-RESEARCH.md (Pattern 5 "Confetti — Imperative, Fire Once" with useRef guard; Pitfall 7 confetti firing on every re-render)
    - .planning/phases/05-guest-app/05-UI-SPEC.md (§"Screen 7: WinnerScreen" — full layout, Trophy icon, #1 featured glass-gold card with `text-gold-bright` winner name, "Câștigător!" heading, "Clasament final" subheading, the exact dynamic-import confetti useEffect with `confettiFired` useRef and colors array; §"Performance Contract" — canvas-confetti MUST be dynamically imported, never static)
    - src/hooks/useGameSync.ts (GameStateSnapshot.leaderboard shape `{ name: string; score: number }[]`; phase === 'ended')
    - src/components/guest/LeaderboardPanel.tsx (created in 05-01 — reuse for the full final list; props `{ leaderboard }`)
    - src/components/guest/SyncStatusBadge.tsx (rendered at top)
    - src/components/ui/card.tsx (Card used for the #1 featured card)
  </read_first>
  <action>
    Create `src/components/guest/WinnerScreen.tsx` ("use client") matching the prop signature page.tsx passes (expected `{ state, status }`). Render per UI-SPEC Screen 7: `min-h-dvh bg-ink flex flex-col items-center gap-8 px-4 pt-12 pb-[env(safe-area-inset-bottom)]`, the `SyncStatusBadge` at top, heading "Câștigător!" (`font-heading text-2xl text-champagne`), a `.thin-divider`, then a `glass-gold` `Card` featuring #1: a `Trophy` icon (lucide, `size-8 text-gold-bright`), the winner name `text-2xl font-bold font-heading text-gold-bright` (from `state.leaderboard[0]?.name`; if the leaderboard is empty render a graceful "Felicitări tuturor!" fallback instead of crashing), and a sub line "{score} răspunsuri corecte" from `state.leaderboard[0]?.score`. Below, a `.thin-divider`, a subheading "Clasament final", and `<LeaderboardPanel leaderboard={state.leaderboard} />` for the full ranked list (PLAY-06 final-state).

    Confetti — exactly once (Pitfall 7): declare `const confettiFired = useRef(false)`. In a `useEffect(() => { ... }, [])` (empty deps), early-return if `confettiFired.current`, else set it true and `import("canvas-confetti").then(({ default: confetti }) => confetti({ particleCount: 120, spread: 70, origin: { y: 0.6 }, colors: ["#f0c060", "#f5e6c8", "#d4a843", "#e8a0a0"] }))`. The dynamic import (NOT a static top-of-file import) is mandatory per the Performance Contract — it keeps canvas-confetti out of the main bundle until the ended phase. WinnerScreen only mounts when phase==='ended' (single mount via the page.tsx conditional), so the empty-deps effect runs once. Do NOT call useGameSync (Pitfall 3).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `WinnerScreen` uses a dynamic `import("canvas-confetti")` inside a `useEffect` — NOT a static top-level `import confetti from "canvas-confetti"`
    - A `useRef(false)` flag (`confettiFired`) guards the confetti call so it fires at most once
    - The #1 featured card reads `state.leaderboard[0]` and renders the winner name in `text-gold-bright`, with a graceful fallback when the leaderboard is empty (no crash on `leaderboard[0]` being undefined)
    - The heading text "Câștigător!" and subheading "Clasament final" are present
    - `WinnerScreen` renders `<LeaderboardPanel leaderboard={state.leaderboard} />`
    - Component does NOT call `useGameSync`
    - `npm run build` exits 0 and `npm run lint` reports no errors
  </acceptance_criteria>
  <done>On game end the guest sees the #1 player featured, the full final leaderboard, and a single confetti burst (dynamically imported, ref-guarded); empty leaderboard degrades gracefully; build + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest browser → (none new) | WinnerScreen is read-only with respect to game state; it only reads `state.leaderboard` from useGameSync. No new mutation surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Denial of Service | confetti re-firing / perf | mitigate | Dynamic import (off main bundle) + useRef(false) guard ensures a single burst; no static import, no per-render call (Pitfall 7) |
| T-05-08 | Tampering | leaderboard display names (XSS) | mitigate | Names rendered as React text content via LeaderboardPanel; no dangerouslySetInnerHTML |
</threat_model>

<verification>
- `npm run build` exits 0; `npm run lint` clean
- Manual (UAT): host ends game → guest sees winner screen with #1 featured + full leaderboard; confetti fires exactly once (does not re-fire as presence/other broadcasts arrive)
</verification>

<success_criteria>
- Guest sees the game-end / winner state live (PLAY-07)
- Full final leaderboard shown ranked; live leaderboard updates between rounds satisfied via LeaderboardPanel (PLAY-06)
- Confetti fires exactly once on entering the ended phase
- Build + lint green
</success_criteria>

<output>
Create `.planning/phases/05-guest-app/05-03-SUMMARY.md` when done
</output>
