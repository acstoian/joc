---
phase: 06-tv-display-mode
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/display/QuestionDisplay.tsx
  - src/components/display/LockedDisplay.tsx
  - src/components/display/RevealDisplay.tsx
autonomous: false
requirements: [DISP-03, DISP-04, DISP-05, DISP-06]
user_setup: []

must_haves:
  truths:
    - "When the host starts the game, the display shows the current question text with a slide-up entry animation (DISP-03, D-02)"
    - "When the host locks answers, the display shows live A/B percentage bars that fill and update as answers arrive from useGameSync re-fetches (DISP-04, D-03)"
    - "On reveal, the correct option card glows gold and scales up; the wrong option dims to 40% opacity (DISP-05, D-04)"
    - "After reveal, a compact top-5 leaderboard appears below the bars (DISP-06, D-05)"
    - "npm run build and npm run lint both exit 0"
  artifacts:
    - path: "src/components/display/QuestionDisplay.tsx"
      provides: "Question screen — slide-up question text + A/B option cards, no bars"
      exports: ["QuestionDisplay"]
      min_lines: 30
    - path: "src/components/display/LockedDisplay.tsx"
      provides: "Locked screen — question + live-filling A/B percentage bars"
      exports: ["LockedDisplay"]
      min_lines: 40
    - path: "src/components/display/RevealDisplay.tsx"
      provides: "Reveal screen — gold-glow correct bar + dimmed wrong + top-5 leaderboard"
      exports: ["RevealDisplay"]
      min_lines: 50
  key_links:
    - from: "src/components/display/LockedDisplay.tsx"
      to: "state.distribution"
      via: "bar width = state.distribution.A / state.distribution.B percentage"
      pattern: "distribution"
    - from: "src/components/display/RevealDisplay.tsx"
      to: "src/components/guest/LeaderboardPanel.tsx"
      via: "imports and renders LeaderboardPanel with state.leaderboard.slice(0,5)"
      pattern: "LeaderboardPanel"
    - from: "src/components/display/RevealDisplay.tsx"
      to: "state.correctOption"
      via: "conditional gold-glow class on the matching option card"
      pattern: "correctOption"
---

<objective>
Ship the live gameplay vertical slice of the TV display: question entry, locked-phase live answer bars, and the reveal with gold-glow correct highlighting plus a top-5 leaderboard. After this plan, a host can drive question → locked → revealed and the TV shows each beat cinematically (within Phase 6's functional-animation scope, D-07).

This plan REPLACES three stub components created in 06-01 (`QuestionDisplay`, `LockedDisplay`, `RevealDisplay`) with their real implementations. It does NOT touch `src/app/display/page.tsx` — the shell already imports and switches to these components, so dropping in the real files requires no page changes (the Phase 5 stub-replacement pattern).

Purpose: Deliver the most visually load-bearing display screens — the question moment, the live vote bars, and the reveal effect — using only CSS transitions and the `slide-up`/`fade-scale` keyframes added in 06-01.

Output: QuestionDisplay (DISP-03), LockedDisplay (DISP-04), RevealDisplay (DISP-05 + DISP-06) — all reading authoritative state from props supplied by the single `useGameSync` call in the shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tv-display-mode/06-CONTEXT.md
@.planning/phases/06-tv-display-mode/06-RESEARCH.md
@.planning/phases/06-tv-display-mode/06-UI-SPEC.md

# Read 06-01 SUMMARY to confirm the shell prop contract + keyframes are in place
@.planning/phases/06-tv-display-mode/06-01-SUMMARY.md

# Patterns + the real state shape (read before implementing)
@src/hooks/useGameSync.ts
@src/components/guest/QuestionScreen.tsx
@src/components/guest/LeaderboardPanel.tsx
@src/lib/utils.ts
@src/app/globals.css
</context>

<artifacts_this_phase_produces>
| Symbol / File | Kind | Consumed by |
|---------------|------|-------------|
| `QuestionDisplay` (real) | component | DisplayPage shell (06-01), phase "question" |
| `LockedDisplay` (real) | component | DisplayPage shell, phase "locked" |
| `RevealDisplay` (real) | component | DisplayPage shell, phase "revealed" |

No new exports beyond these three components. No new shared helpers required (an internal `OptionCard` / `OptionWithBar` may be defined locally inside each file, but keep it within these three files; do NOT create files outside `files_modified`).
</artifacts_this_phase_produces>

<critical_corrections>
Follow these — the UI-SPEC has factual errors about the state shape (verified against `src/hooks/useGameSync.ts`):

1. **Question text field is `state.currentQuestion.body`, NOT `.text`.** The real `currentQuestion` shape is `{ id: string; body: string; optionA: string; optionB: string }`. The guest `QuestionScreen.tsx` (line 129) already uses `q?.body`. The UI-SPEC's `state.currentQuestion.text` is WRONG — use `.body`.

2. **A/B option text is `state.currentQuestion.optionA` / `.optionB`** — these ARE correct in the UI-SPEC.

3. **There is NO question-index field.** Use the static label `"Întrebarea"` (matching guest QuestionScreen) — do NOT render "Întrebarea {N}" with an invented index.

4. **`state.distribution` can be `null`** (typed `{ A: number; B: number } | null`). In LockedDisplay/RevealDisplay, guard: `const dist = state.distribution ?? { A: 0, B: 0 };` before reading `.A`/`.B`. Render `dist.A` as the bar width percentage and "{dist.A}%" label for option A, `dist.B` for option B per UI-SPEC §LockedDisplay (which uses `pct={state.distribution.A}`).

5. **`state.correctOption` is `"A" | "B" | null`** — populated only in the revealed phase. In RevealDisplay, guard for null but it will be set; use it to pick which option card gets the gold treatment.

6. **`state.currentQuestion` can be `null`.** Guard with optional chaining and a fallback string `"Se încarcă întrebarea..."` (matches guest QuestionScreen).
</critical_corrections>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: QuestionDisplay — slide-up question + A/B option cards (no bars)</name>
  <files>src/components/display/QuestionDisplay.tsx</files>
  <read_first>
    - src/components/display/QuestionDisplay.tsx (the 06-01 stub being replaced — confirm prop signature `{ state: GameStateSnapshot }`)
    - src/components/guest/QuestionScreen.tsx (real `.body` / `.optionA` / `.optionB` usage; static "Întrebarea" label)
    - src/hooks/useGameSync.ts (GameStateSnapshot.currentQuestion shape: { id, body, optionA, optionB })
    - src/app/globals.css (the slide-up keyframe added in 06-01)
    - .planning/phases/06-tv-display-mode/06-UI-SPEC.md (§Screen 2: QuestionDisplay — typography table, token classes)
    - src/lib/utils.ts (cn)
  </read_first>
  <action>
    Replace the QuestionDisplay stub with the real component (D-02, DISP-03). `"use client"`, `export function QuestionDisplay({ state }: { state: GameStateSnapshot })`.

    Layout per UI-SPEC §Screen 2 (full-screen centered column `flex min-h-dvh flex-col items-center justify-center gap-[5vh] px-[5vw]`):
    - Question phase label: `<p className="text-[1.5vw] font-normal font-body text-champagne-dim/70 text-center uppercase tracking-widest">Întrebarea</p>` (static label — no index, per correction 3).
    - Question text `<h2>`: use `key={state.currentQuestionId}` so React re-mounts it (and re-fires the slide-up animation) on every question change. Classes `text-[6vw] font-bold font-heading text-champagne text-center leading-snug max-w-[80vw] [animation:slide-up_400ms_ease-out_forwards]`. Content `{state.currentQuestion?.body ?? "Se încarcă întrebarea..."}` (correction 1 + 6).
    - A/B option cards: a `grid grid-cols-2 gap-[3vw] w-full max-w-[90vw]` containing two option cards (define a local `OptionCard` helper or inline). Each card: `glass rounded-2xl flex flex-col items-center justify-center gap-[1.5vh] px-[4vw] py-[3vh] min-h-[18vh]` with a letter-prefix span (`text-[1.5vw] font-normal font-body text-champagne-dim/70 uppercase tracking-[0.3em]`, "A" / "B") and the option text span (`text-[2vw] font-normal font-body text-champagne text-center`) reading `state.currentQuestion?.optionA ?? "A"` and `?.optionB ?? "B"`.
    - NO bars in this phase (D-02 — suspense before lock).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/display/QuestionDisplay.tsx` exports `QuestionDisplay` with prop `{ state: GameStateSnapshot }`
    - The question `<h2>` has `key={state.currentQuestionId}` and the `[animation:slide-up_...]` class
    - The component reads `state.currentQuestion?.body` (not `.text`)
    - The phase label is the static string "Întrebarea" (no `{N}` index)
    - Two A/B option cards render with `optionA` / `optionB` text and no percentage bars
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>QuestionDisplay shows the live question text with a slide-up entry and two clean A/B option cards (no bars), reading the correct `.body`/`.optionA`/`.optionB` fields.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: LockedDisplay (live bars) + RevealDisplay (gold glow + top-5 leaderboard)</name>
  <files>src/components/display/LockedDisplay.tsx, src/components/display/RevealDisplay.tsx</files>
  <read_first>
    - src/components/display/LockedDisplay.tsx and src/components/display/RevealDisplay.tsx (06-01 stubs being replaced)
    - src/components/display/QuestionDisplay.tsx (Task 1 — reuse the OptionCard layout / "Întrebarea" label / question `<h2>`)
    - src/hooks/useGameSync.ts (distribution: { A, B } | null; correctOption: "A"|"B"|null; leaderboard: { name, score }[])
    - src/components/guest/LeaderboardPanel.tsx (import as-is, prop is `{ leaderboard }`)
    - .planning/phases/06-tv-display-mode/06-UI-SPEC.md (§Screen 3 LockedDisplay, §Screen 4 RevealDisplay — OptionWithBar correct/incorrect classes, after-reveal leaderboard)
    - .planning/phases/06-tv-display-mode/06-RESEARCH.md (Finding 5 — LeaderboardPanel scale wrapper; Pitfall 5 — apply reveal classes in JSX not useEffect)
    - src/lib/utils.ts (cn)
  </read_first>
  <action>
    Replace the LockedDisplay and RevealDisplay stubs with real components.

    (a) `src/components/display/LockedDisplay.tsx` — `"use client"`, `export function LockedDisplay({ state }: { state: GameStateSnapshot })` (D-03, DISP-04). Reuse the QuestionDisplay layout (question label + `<h2>` question body — same classes, but NO slide-up animation on the locked phase change; render the `<h2>` without the animation class). Add a locked notice `<p className="text-[1.5vw] font-normal font-body text-champagne-dim/60 text-center">Răspunsurile au fost blocate</p>`. Below, a `grid grid-cols-2 gap-[3vw] w-full max-w-[90vw]` of `OptionWithBar` for A and B. Each `OptionWithBar` = the glass OptionCard + a percentage row below it: a bar track `flex-1 h-[1.5vh] rounded-full bg-ink-muted overflow-hidden` containing a fill `h-full rounded-full bg-gold transition-[width] duration-500 ease-out` with inline `style={{ width: \`${pct}%\` }}`, and a "{pct}%" label `text-[2vw] font-bold font-body text-champagne w-[5vw] text-right shrink-0`. Guard distribution: `const dist = state.distribution ?? { A: 0, B: 0 };` then `pct` is `dist.A` for option A, `dist.B` for option B (correction 4). The `transition-[width]` makes bars animate as the value updates on each useGameSync re-fetch.

    (b) `src/components/display/RevealDisplay.tsx` — `"use client"`, `export function RevealDisplay({ state }: { state: GameStateSnapshot })` (D-04, D-05, DISP-05, DISP-06). Same question + options + bars as LockedDisplay, with reveal styling applied via CONDITIONAL CLASSES IN THE JSX (no useEffect — Pitfall 5). Define an `OptionWithBar` that takes `option`, `text`, `pct`, and `correctOption = state.correctOption`. Compute `const isCorrect = option === correctOption;`. Correct card: `glass-gold border-2 border-gold-bright shadow-[0_0_40px_0_rgba(240,192,96,0.45)] scale-[1.03] transition-transform duration-300` and option text `text-gold-bright`; its bar fill uses `bg-gold-bright`. Incorrect wrapper: add `opacity-40 transition-opacity duration-300`; bar fill stays `bg-gold`. Use `cn()` for the conditional classes.

    After the bars, render the after-reveal top-5 leaderboard (D-05, DISP-06): a `.thin-divider` then the Phase 5 `LeaderboardPanel` imported from `@/components/guest/LeaderboardPanel`, wrapped in a scaling container per Finding 5 so it is readable on TV: `<div className="w-full max-w-[80vw] mx-auto mt-[4vh] transform scale-150 origin-top"><LeaderboardPanel leaderboard={state.leaderboard.slice(0, 5)} /></div>`. Import `LeaderboardPanel` unmodified (do not edit the guest component).

    To avoid duplication, you MAY define a single internal `OptionWithBar` helper used by both LockedDisplay and RevealDisplay, but keep all code within the two files listed in `files_modified` — do NOT create a new shared file outside this plan's file ownership.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `LockedDisplay` and `RevealDisplay` export their named components with prop `{ state: GameStateSnapshot }`
    - LockedDisplay bar fill divs use `transition-[width]` and inline `width: ${pct}%`
    - LockedDisplay guards `state.distribution` against null (no crash when distribution is null)
    - RevealDisplay applies the gold-glow classes (`border-gold-bright`, `shadow-[0_0_40px`, `scale-[1.03]`) to the card matching `state.correctOption` via a JSX conditional (no useEffect)
    - RevealDisplay dims the non-correct option wrapper with `opacity-40`
    - RevealDisplay imports `LeaderboardPanel` from `@/components/guest/LeaderboardPanel` and renders `state.leaderboard.slice(0, 5)` inside a scale wrapper
    - `src/components/guest/LeaderboardPanel.tsx` is NOT modified
    - No new files created outside `files_modified`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>LockedDisplay shows live A/B bars that animate as votes arrive; RevealDisplay highlights the correct option in gold, dims the wrong one, and shows a readable top-5 leaderboard below.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — question entry, live bars, gold reveal + leaderboard</name>
  <action>Pause for human verification. The executor has completed QuestionDisplay, LockedDisplay, and RevealDisplay (Tasks 1-2); this checkpoint verifies question entry, live answer bars, and the gold reveal + leaderboard across host-driven phase transitions. No code is written in this task — follow the steps below and wait for the resume signal.</action>
  <what-built>
    The three live gameplay display screens: QuestionDisplay (slide-up question + A/B cards), LockedDisplay (live-filling A/B percentage bars), and RevealDisplay (gold-glow correct option + dimmed wrong option + top-5 leaderboard). All driven by the single useGameSync subscription in the shell — no display changes needed for these to appear.
  </what-built>
  <how-to-verify>
    1. `npm run dev`. Ensure the host game has at least 2 questions with a correct option set (Host → Intrebari tab).
    2. Open `/display` in one tab (laptop/TV) and `/host` in another. Optionally open `/` on a phone and join so there are answers to show.
    3. Host: click "Porneste Jocul". On the display, the question text should slide up from below into place, with two clean A/B option cards and NO bars (DISP-03).
    4. From the phone (and/or more devices), tap A or B to register answers.
    5. Host: click "Blocheaza Raspunsurile". The display should now show A/B percentage bars that reflect the votes; if more answers were registered they fill smoothly (DISP-04).
    6. Host: pick the correct option (A or B picker) and click "Dezvaluie Raspunsul". On the display, the correct option card should glow gold and scale up slightly; the wrong option should dim to ~40% opacity; a top-5 leaderboard should appear below the bars, readable from a few metres (DISP-05, DISP-06).
    7. Confirm the connection dot stays green throughout and no manual refresh was needed.
  </how-to-verify>
  <resume-signal>Type "approved" if question entry, live bars, and the gold reveal + leaderboard all render correctly — or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Broadcast → display client | Display re-fetches authoritative state via useGameSync; question/distribution/leaderboard data always comes from GET /api/game/state, never the broadcast payload |
| game state (player names) → rendered DOM | Leaderboard and option text are user/host-authored strings rendered into the display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | leaderboard player names + question/option text in QuestionDisplay/LockedDisplay/RevealDisplay | mitigate | All strings rendered as React text nodes; no `dangerouslySetInnerHTML` anywhere — XSS-safe (inherited Phase 5 control) |
| T-06-05 | Information Disclosure | correctOption shown on display | accept | Intentional — reveal phase is when the correct option is meant to be public; the display reads `state.correctOption` which the server only populates in the revealed phase (Phase 3 phase-gating) |
| T-06-SC | Tampering | npm installs | mitigate | No new packages — reuses LeaderboardPanel and existing deps only |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run lint` exits 0
- `grep -n "currentQuestion.body\|currentQuestion?.body" src/components/display/QuestionDisplay.tsx` returns a match; `grep -rn "currentQuestion.text\|currentQuestion?.text" src/components/display/` returns nothing
- `grep -n "LeaderboardPanel" src/components/display/RevealDisplay.tsx` returns a match
- Manual checkpoint: question slide-up, live locked bars, gold reveal + top-5 leaderboard all render (Task 3)
</verification>

<success_criteria>
- DISP-03: question screen shows the current question with a slide-up entry animation
- DISP-04: locked phase shows live A/B percentage bars that fill as answers arrive
- DISP-05: reveal highlights the correct option with a gold glow + scale and dims the wrong option
- DISP-06: a leaderboard (top-5) appears after the reveal
- Guest `LeaderboardPanel` reused unmodified; no files created outside the three owned by this plan
- Build and lint green
</success_criteria>

<output>
Create `.planning/phases/06-tv-display-mode/06-02-SUMMARY.md` when done.
</output>
