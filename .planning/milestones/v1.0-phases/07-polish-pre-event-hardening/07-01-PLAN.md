---
phase: 07-polish-pre-event-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/display/page.tsx
autonomous: true
requirements: [RT-07]
must_haves:
  truths:
    - "A reusable .text-gradient-gold CSS utility exists and is consumable by component class strings"
    - "On the TV display, advancing a game phase slides the old screen up/out and the new screen up/in (slide+fade), with no overlap"
    - "Devices with prefers-reduced-motion enabled see flat gold text (not invisible) and instant phase swaps (no animation)"
  artifacts:
    - path: "src/app/globals.css"
      provides: ".text-gradient-gold utility + reduced-motion override"
      contains: ".text-gradient-gold"
    - path: "src/app/display/page.tsx"
      provides: "AnimatePresence mode=wait wrapping the phase screen variable"
      contains: "AnimatePresence"
  key_links:
    - from: "src/app/display/page.tsx"
      to: "motion/react"
      via: "import AnimatePresence, motion, useReducedMotion"
      pattern: "from \"motion/react\""
    - from: "src/app/display/page.tsx"
      to: "state.phase"
      via: "AnimatePresence child key"
      pattern: "key=\\{state\\?\\.phase"
---

<objective>
Establish the two foundation pieces every other Phase 7 polish slice depends on: the reusable `.text-gradient-gold` CSS utility (consumed by 4 hero headings across the guest and TV surfaces) and the TV display phase-transition shell (`AnimatePresence mode="wait"` wrapping the phase `screen` variable in `display/page.tsx`).

This is a vertical slice: after this plan, a real user watching the TV sees cinematic slide+fade transitions between game phases (start → question → locked → revealed → ended), and the gold-gradient text utility is available for the component slices that follow. Per D-01, AnimatePresence is introduced ONLY to the TV display surface — the guest app stays CSS-only.

Purpose: RT-07 (soft-luxury aesthetic) — smooth TV phase transitions + the shared gradient utility.
Output: `.text-gradient-gold` utility in globals.css (with reduced-motion override), AnimatePresence-wrapped phase screen in display/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-pre-event-hardening/07-CONTEXT.md
@.planning/phases/07-polish-pre-event-hardening/07-RESEARCH.md
@.planning/phases/07-polish-pre-event-hardening/07-UI-SPEC.md
@.planning/phases/07-polish-pre-event-hardening/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .text-gradient-gold CSS utility + reduced-motion override to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css — read the full file to locate the `.glass-gold` block (around lines 63-68) and the existing `@media (prefers-reduced-motion: reduce)` block (around lines 90-94). The new utility goes after `.glass-gold` and before `.thin-divider`; the override goes inside the existing reduced-motion block.
    - .planning/phases/07-polish-pre-event-hardening/07-PATTERNS.md §"src/app/globals.css" — exact insertion points and code.
  </read_first>
  <action>
    Add a new CSS utility class `.text-gradient-gold` immediately after the existing `.glass-gold` block (and before `.thin-divider`). It sets `background: linear-gradient(135deg, var(--color-gold), var(--color-champagne));` plus `background-clip: text;`, `-webkit-background-clip: text;`, and `color: transparent;`. Use the existing `@theme` CSS variables `var(--color-gold)` and `var(--color-champagne)` — no hex literals. This is a STATIC gradient per D-07; do NOT add any `@keyframes` or `animation` property to it.

    Inside the EXISTING `@media (prefers-reduced-motion: reduce)` block (do not create a second one), add a `.text-gradient-gold` override that sets `background: none;` and `color: var(--color-gold);`. This is mandatory (Pitfall 7): without it, `color: transparent` makes the text invisible on devices with reduced motion enabled.

    Do not modify any other rule in globals.css. Preserve the existing `.glass`, `.glass-gold`, `slide-up`/`fade-scale` keyframes, and all other reduced-motion overrides exactly.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains a `.text-gradient-gold` rule with `background-clip: text` and `color: transparent`
    - `src/app/globals.css` contains `linear-gradient(135deg, var(--color-gold), var(--color-champagne))`
    - The `@media (prefers-reduced-motion: reduce)` block contains a `.text-gradient-gold` override with `color: var(--color-gold)` and `background: none`
    - `.text-gradient-gold` contains NO `animation` or `@keyframes` reference (static gradient per D-07)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The `.text-gradient-gold` utility exists in globals.css with a reduced-motion override; build passes; no keyframe animation on the utility.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap TV display phase screen in AnimatePresence mode="wait" (slide+fade, D-01/D-02)</name>
  <files>src/app/display/page.tsx</files>
  <read_first>
    - src/app/display/page.tsx — read the full file. Confirm current state: `{screen}` is rendered as a bare JSX expression (around line 110); `useGameSync` is called (around line 42); `containerRef`, `isFullscreen` state, `HOST_SENTINEL_PLAYER_ID`, the fullscreen change listener, `DisplayStatusDot`, and the fullscreen button are all present and MUST be preserved.
    - .planning/phases/07-polish-pre-event-hardening/07-PATTERNS.md §"src/app/display/page.tsx" — exact import additions, the `useReducedMotion` placement, and the replacement JSX for `{screen}`.
    - .planning/phases/07-polish-pre-event-hardening/07-UI-SPEC.md §"Animation 1" — the variants, transition, and `mode="wait"` contract.
  </read_first>
  <action>
    Add `import { AnimatePresence, motion, useReducedMotion } from "motion/react";` to the existing imports. In the `DisplayPage` component body, after the existing `useGameSync` call, add `const shouldReduce = useReducedMotion();`.

    Replace the bare `{screen}` render with an `<AnimatePresence mode="wait">` wrapping a single `<motion.div>`. The `motion.div` must use `key={state?.phase ?? "loading"}` (the phase string is the exact AnimatePresence trigger — Pitfall 2). Apply `variants` with `hidden: { opacity: 0, y: "2vh" }`, `visible: { opacity: 1, y: 0 }`, `exit: { opacity: 0, y: "-1vh" }`; set `initial="hidden"`, `animate="visible"`, `exit="exit"`. Set `transition={{ duration: shouldReduce ? 0 : 0.35, ease: "easeInOut" }}` so reduced-motion users get instant swaps. Apply `className="w-full h-full"` to the motion.div. Render `{screen}` as the motion.div child.

    Animate ONLY `opacity` and `transform` (translateY via the `y` variant) — never `width`, `height`, `top`, or `left` (Performance Contract; required for the 60fps 4x-CPU-throttle gate in plan 04). `mode="wait"` ensures the exiting screen fully leaves before the entering screen begins (no overlap).

    Preserve everything else in the file unchanged: `containerRef`, `isFullscreen`, `HOST_SENTINEL_PLAYER_ID`, the fullscreen change listener, `DisplayStatusDot`, the fullscreen button, and the countdown overlay. Do NOT modify any of the individual screen components — AnimatePresence in this file provides the transition for all of them.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/display/page.tsx` imports `AnimatePresence`, `motion`, and `useReducedMotion` from `"motion/react"`
    - `src/app/display/page.tsx` contains `AnimatePresence mode="wait"`
    - The wrapping motion.div uses `key={state?.phase ?? "loading"}`
    - The variants animate only `opacity` and `y` (no `width`/`height`/`top`/`left` in the variants)
    - `transition` uses `shouldReduce ? 0 : 0.35`
    - The fullscreen button and `DisplayStatusDot` references still exist in the file (preservation check)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The TV display phase screen is wrapped in `AnimatePresence mode="wait"` keyed on `state?.phase`, animating opacity+translateY only, with a reduced-motion duration-0 fallback; all prior shell elements preserved; build passes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created or first-introduced by THIS plan (exclude from drift verification):
- CSS class `.text-gradient-gold` (new utility in `src/app/globals.css`) + its `prefers-reduced-motion` override
- `shouldReduce` local const (from `useReducedMotion()`) in `src/app/display/page.tsx`
- `AnimatePresence` / `motion` / `useReducedMotion` imports (from `motion/react`) newly added to `src/app/display/page.tsx`
- The `<AnimatePresence mode="wait">` + `<motion.div>` wrapper around the `{screen}` variable in `src/app/display/page.tsx`

Pre-existing symbols this plan consumes (NOT new): `var(--color-gold)`, `var(--color-champagne)`, `.glass-gold`, `slide-up`/`fade-scale` keyframes, the existing `@media (prefers-reduced-motion: reduce)` block (all in globals.css); `useGameSync`, `state.phase`, `screen`, `containerRef`, `isFullscreen`, `HOST_SENTINEL_PLAYER_ID`, `DisplayStatusDot`, the fullscreen button (all in display/page.tsx).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

Phase 7 (this plan) introduces NO new trust boundaries. Evidence:
- No new API routes (this plan modifies a CSS file and a client display page)
- No new user input (no forms, no params, no body parsing)
- No auth or session changes
- No new client-side Supabase queries (the display page already uses the existing `useGameSync` hook unchanged)

| Boundary | Status |
|----------|--------|
| client → API | unchanged — no new routes or fetches added |
| anon role → DB | unchanged — no new queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `.text-gradient-gold` CSS utility | accept | Pure CSS, no JavaScript, no `dangerouslySetInnerHTML`; no XSS vector (RESEARCH.md §Security Domain) |
| T-07-02 | Information Disclosure | `display/page.tsx` AnimatePresence wrap | accept | No new data access; reads only existing `state.phase`; service-role key remains server-only (invariant maintained) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | NO new packages installed in Phase 7 — `motion` and `canvas-confetti` already present and audited in prior phases (RESEARCH.md §Package Legitimacy Audit). No install task exists in this plan. |

ASVS L1 conclusion: Phase 7 adds no new attack surface (no routes, no auth changes, no new user input — confirmed in RESEARCH.md §Security Domain). All threats are accepted with documented rationale.
</threat_model>

<verification>
- `npm run build` exits 0 after each task.
- Visual (manual, deferred to plan 04 perf/dry-run gate): on `npm run dev`, advancing TV phases shows slide+fade transitions; the gradient utility is visually confirmed once consumed by plan 02/03.
- Reduced-motion: with OS reduce-motion enabled, TV phase swaps are instant and `.text-gradient-gold` renders flat gold (not invisible).
</verification>

<success_criteria>
- `.text-gradient-gold` utility and its reduced-motion override exist in globals.css; the utility uses CSS variables, has no keyframe animation.
- `display/page.tsx` wraps `{screen}` in `AnimatePresence mode="wait"` keyed on `state?.phase`, animating opacity+translateY only, with a reduced-motion duration-0 fallback.
- All prior display-page shell elements (fullscreen, status dot, countdown) preserved.
- `npm run build` exits 0.
</success_criteria>

<output>
Create `.planning/phases/07-polish-pre-event-hardening/07-01-SUMMARY.md` when done.
</output>
