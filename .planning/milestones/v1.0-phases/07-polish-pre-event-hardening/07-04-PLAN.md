---
phase: 07-polish-pre-event-hardening
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified: []
autonomous: false
requirements: [RT-08]
user_setup:
  - service: vercel
    why: "RT-08 dry run requires the latest Phase 7 code deployed to production (not localhost)"
    dashboard_config:
      - task: "Deploy latest Phase 7 build to production"
        location: "Vercel — joc project"
  - service: supabase
    why: "RT-08 dry run requires Supabase Pro plan active (free tier is unsafe for 100+ concurrent connections — STATE.md known blocker)"
    dashboard_config:
      - task: "Confirm Supabase project is on the Pro plan before the dry run"
        location: "Supabase Dashboard — Settings → Billing"
must_haves:
  truths:
    - "On the guest A/B tap screen under Chrome DevTools 4x CPU throttle, tapping A/B holds ~60fps"
    - "No motion/react variant in any Phase 7 component animates layout properties (width/height/top/left)"
    - "The leaderboard stagger fires once per reveal/winner mount, not on every score tick"
    - "A full end-to-end game on production with 5+ real devices (including an iPhone on Safari) completes with no stuck states, no sync gaps, and verified emergency controls"
  artifacts:
    - path: ".planning/phases/07-polish-pre-event-hardening/07-04-SUMMARY.md"
      provides: "Recorded performance result + completed dry-run checklist"
      contains: "Dry Run"
  key_links:
    - from: "guest A/B tap screen"
      to: "60fps under 4x CPU throttle"
      via: "compositor-only animations (opacity + transform)"
      pattern: "DevTools Performance"
---

<objective>
Close Phase 7 — and the project's last open gate — by hardening the polished build against the two RT-08 non-negotiables: a performance audit proving the guest A/B tap screen holds 60fps under throttle with no layout-triggering animations, and the MANDATORY end-to-end production dry run on 5+ real devices (including an iPhone on Safari) that must pass before the wedding.

This is the final vertical slice: after this plan, the team has objective evidence the live experience is fast and the full game flow survives real devices on production infrastructure. The dry run is a HARD GATE (Pitfall 8) — it is the last task in the phase and pauses for human verification; the wedding ships only if it passes.

Purpose: RT-08 (pre-event production dry run validates concurrency, reconnect, and host flow on real devices) + the 60fps performance contract.
Output: a recorded performance result (60fps under 4x CPU throttle, no layout triggers) and a completed Dry Run Protocol checklist captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-pre-event-hardening/07-CONTEXT.md
@.planning/phases/07-polish-pre-event-hardening/07-RESEARCH.md
@.planning/phases/07-polish-pre-event-hardening/07-UI-SPEC.md
@.planning/phases/07-polish-pre-event-hardening/07-01-SUMMARY.md
@.planning/phases/07-polish-pre-event-hardening/07-02-SUMMARY.md
@.planning/phases/07-polish-pre-event-hardening/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static performance audit — no layout-triggering animations; stagger fires once</name>
  <files></files>
  <read_first>
    - src/app/display/page.tsx — confirm the AnimatePresence variants animate only `opacity` and `y` (transform), never `width`/`height`/`top`/`left`.
    - src/components/display/RevealDisplay.tsx, src/components/display/WinnerDisplay.tsx — confirm `containerVariants`/`rowVariants` animate only `opacity`/`y`; confirm the stagger `initial="hidden"`/`animate="visible"` has NO dependency on `state.leaderboard` (so it cannot re-run on score ticks — Pitfall 5).
    - src/components/guest/QuestionScreen.tsx — confirm `whileTap` animates `scale` (transform) only.
    - .planning/phases/07-polish-pre-event-hardening/07-UI-SPEC.md §"Performance Contract" and §"Animation Summary Table" — the compositor-safe rules.
    - .planning/phases/07-polish-pre-event-hardening/07-RESEARCH.md §"Common Pitfalls" 5 and the anti-pattern "Animating layout properties".
  </read_first>
  <action>
    Perform a source-level performance audit of every Phase 7 motion/react animation (no code changes expected — this is a verification task; if a violation is found, fix it and note it in the SUMMARY).

    Verify three properties:
    1. No animated `motion/react` variant or `whileTap`/`transition` in any Phase 7 component animates `width`, `height`, `top`, or `left`. Only `opacity` and `transform` (`y`/`scale`) are permitted. The Phase 6 distribution bar `transition-[width]` is the one pre-existing exception (already benchmarked) and is out of scope.
    2. The leaderboard stagger in RevealDisplay and WinnerDisplay is mount-driven only: `initial="hidden"`/`animate="visible"` with NO `state.leaderboard` in any effect/dependency that would re-trigger it. Confirm AnimatePresence (display/page.tsx) is what remounts the component on phase change.
    3. `canvas-confetti` is dynamically imported (never a static top-level import) in RevealScreen and WinnerDisplay.

    Run `npm run build && npm run lint` to confirm the full polished build compiles and lints clean. Record the audit result (pass, or any fix applied) in the plan SUMMARY.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Grep across the Phase 7 modified components finds no `width`/`height`/`top`/`left` inside any motion variant or animation prop (excluding the pre-existing distribution-bar `transition-[width]`)
    - RevealDisplay and WinnerDisplay stagger has no `state.leaderboard` dependency that re-triggers the animation
    - `RevealScreen.tsx` and `WinnerDisplay.tsx` use `import("canvas-confetti")` (dynamic), with no static `import confetti from "canvas-confetti"` at file top
    - `npm run build && npm run lint` exits 0
    - SUMMARY records the audit outcome
  </acceptance_criteria>
  <done>Source audit confirms all Phase 7 animations are compositor-safe (opacity + transform only), the leaderboard stagger is mount-driven (single-fire per reveal), and confetti is dynamically imported; build + lint pass; result recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 60fps verification on guest A/B tap screen (Chrome DevTools 4x CPU throttle)</name>
  <files>(verification only — no source files modified)</files>
  <action>
    Human-verify checkpoint (no code change). The human runs the guest A/B tap screen under Chrome DevTools Performance with 4x CPU slowdown and confirms the frame rate holds ~60fps with no layout thrash, and that the TV leaderboard stagger fires once per reveal. Detailed steps are in the how-to-verify block. If a dropped-frame/jank issue is found, capture it for a fix before the dry run.
  </action>
  <what-built>
    Phase 7 added `whileTap` scale to the guest A/B buttons, a gold-gradient heading, and (on the TV) AnimatePresence transitions + staggered leaderboards + confetti — all using compositor-only animations (opacity + transform). Task 1 statically confirmed no layout-triggering properties are animated. This checkpoint confirms the runtime frame rate on the most performance-sensitive surface (the guest tap screen) under a low-end-phone simulation.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the guest app in Chrome.
    2. Drive the game (via the host dashboard or a second tab) so the guest reaches the question (A/B tap) screen.
    3. Open Chrome DevTools → Performance tab. Set CPU to "4x slowdown" (throttle). Optionally set Network to a throttled profile.
    4. Start a Performance recording, tap A and B several times (and observe the gold-gradient join heading + a phase transition or two on a TV `/display` tab if convenient), then stop the recording.
    5. Inspect the recording: confirm the frame rate holds at/near 60fps during taps (green frames, no long red "dropped frame" bars), and that there are no "Recalculate Style / Layout" thrash spikes tied to the tap animation.
    6. Confirm the leaderboard stagger on `/display` runs exactly once when entering the reveal/winner phase (not re-running on score updates within the phase).
  </how-to-verify>
  <verify>
    <human-check>Guest A/B tap screen holds ~60fps under 4x CPU throttle; no layout thrash; leaderboard stagger fires once per reveal.</human-check>
  </verify>
  <done>The guest A/B tap screen holds ~60fps under 4x CPU throttle with no layout/style thrash, and the TV leaderboard stagger fires once per reveal — confirmed by the human via a Chrome DevTools Performance recording; result noted in the SUMMARY.</done>
  <resume-signal>Type "approved" (and the observed fps / any notes), or describe the jank/dropped-frame issue so it can be fixed before the dry run.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: MANDATORY production dry run — 5+ real devices incl. iPhone/Safari (RT-08 hard gate)</name>
  <files>(verification only — no source files modified)</files>
  <action>
    Human-only gate (no code change). The human runs the full 13-step Dry Run Protocol against the PRODUCTION deployment (Vercel + Supabase Pro) with 5+ real devices including an iPhone on Safari, and confirms every check passes (no stuck states, no sync gaps, emergency controls verified). Prerequisites and the full checklist are in the how-to-verify block. If any check fails, capture the failure so a gap-closure plan can be created before the wedding. This gate cannot be auto-approved.
  </action>
  <what-built>
    The complete, polished game (all six prior phases + the Phase 7 aesthetic/animation layer) deployed to production. This is the final, non-negotiable validation before the wedding: a full end-to-end game on production infrastructure (Vercel + Supabase Pro, NOT localhost) with 5+ real devices including at least one iPhone on Safari. This is a HARD GATE (Pitfall 8 + STATE.md blocker) — the event ships only if all checks pass.
  </what-built>
  <how-to-verify>
    PREREQUISITES (human-only — Claude cannot perform these):
    - Production Vercel deployment is live with the latest Phase 7 code (plans 01-03 merged + deployed).
    - Supabase project is on the Pro plan (free tier is unsafe for 100+ concurrent connections — STATE.md known blocker).
    - 5+ real devices available, including at least one iPhone running Safari.
    - Host device prepared with credentials.

    DRY RUN CHECKLIST (run against the PRODUCTION URL, not localhost — all 13 must pass):
    1. TV display opens at `/display`, enters fullscreen → LobbyDisplay shows with participant count.
    2. 5+ guests join from real phones via name entry → all names appear; count increments live.
    3. Host starts game → all phones + TV transition to the question screen.
    4. All guests tap A or B → lock feedback visible (incl. the 0.96 tap scale); host dashboard shows answer distribution.
    5. Host locks answers → all phones show "Aștepți dezvăluirea...".
    6. Host reveals → TV shows RevealDisplay with the stagger animation; guest phones show correct/wrong feedback.
    7. Correct-answer guests see the mini confetti burst on RevealScreen.
    8. Host advances to next question → TV animates to the next question (slide+fade).
    9. Repeat for 2-3 questions → no stuck states, no sync gaps.
    10. Host ends game → TV shows WinnerDisplay with confetti + staggered leaderboard.
    11. One guest goes airplane-mode ~30s then reconnects → re-links to current game state correctly.
    12. iPhone Safari screen-lock test: lock the phone 60s, unlock → game state syncs on tab return.
    13. Host uses an emergency reset control → game recovers; all clients resync.

    PASS CRITERIA: all 13 checks pass with no manual server restarts, no stuck clients, no phase desync.
  </how-to-verify>
  <verify>
    <human-check>All 13 Dry Run Protocol checks pass on production (Vercel + Supabase Pro) with 5+ real devices including an iPhone on Safari; no stuck states, no sync gaps, emergency controls verified.</human-check>
  </verify>
  <done>All 13 Dry Run Protocol checks pass on the production deployment (Vercel + Supabase Pro) with 5+ real devices including an iPhone on Safari — no stuck states, no sync gaps, emergency controls verified; the completed checklist (devices used, iPhone/Safari confirmation, anomalies) is recorded in the SUMMARY. Any failed check is captured for a gap-closure plan instead.</done>
  <resume-signal>Type "approved — dry run passed" with the checklist results (devices used, iPhone/Safari confirmed, any anomalies), OR list the failed check(s) so a gap-closure plan can be created before the wedding. This gate cannot be auto-approved.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created or first-introduced by THIS plan (exclude from drift verification):
- `.planning/phases/07-polish-pre-event-hardening/07-04-SUMMARY.md` — records the performance audit result and the completed Dry Run Protocol checklist outcome

This plan modifies no source files (it is a verification + human-gate plan). It consumes the polished artifacts from plans 01-03 and the production Vercel/Supabase Pro environment.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

This plan introduces NO new trust boundaries and NO code (it is a verification + dry-run plan). The only relevant boundary is the production deployment itself, which was established in prior phases and is unchanged here.

| Boundary | Status |
|----------|--------|
| client → API (production) | unchanged — dry run exercises the existing, already-tested routes |
| anon role → Supabase (production) | unchanged — verifies RLS/isolation already shipped in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Denial of Service | Production concurrency (100+ guests) | mitigate | Supabase Pro plan REQUIRED before the dry run (free tier unsafe for 100+ concurrent — STATE.md blocker); dry run validates real-device concurrency before the event |
| T-07-08 | Repudiation / availability | Reconnect + emergency recovery under real network | mitigate | Dry run checks 11-13 (airplane-mode reconnect, iPhone Safari screen-lock, host emergency reset) prove recovery paths work on production before the wedding |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | NO new packages introduced in Phase 7 (RESEARCH.md §Package Legitimacy Audit). No install task in this plan. |

ASVS L1 conclusion: Phase 7 adds no new attack surface. This plan validates that the previously-shipped security boundaries (RLS, key isolation, phase guards) still hold under real production load. All threats either accepted or mitigated by the dry-run protocol.
</threat_model>

<verification>
- `npm run build && npm run lint` exits 0 (Task 1).
- 60fps under 4x CPU throttle confirmed via Chrome DevTools Performance (Task 2 human-verify).
- All 13 Dry Run Protocol checks pass on production with 5+ real devices incl. iPhone/Safari (Task 3 blocking-human gate).
</verification>

<success_criteria>
- No Phase 7 animation animates layout properties; leaderboard stagger fires once per reveal; confetti dynamically imported (Task 1).
- Guest A/B tap screen holds ~60fps under 4x CPU throttle with no layout thrash (Task 2).
- The mandatory production dry run passes all 13 checks on Vercel + Supabase Pro with 5+ real devices including an iPhone on Safari — no stuck states, no sync gaps, emergency controls verified (Task 3).
</success_criteria>

<output>
Create `.planning/phases/07-polish-pre-event-hardening/07-04-SUMMARY.md` when done — include the recorded performance result and the completed Dry Run Protocol checklist (devices used, iPhone/Safari confirmation, anomalies).
</output>
