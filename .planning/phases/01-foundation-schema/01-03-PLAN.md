---
phase: 01-foundation-schema
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/app/page.tsx
  - src/app/api/skeleton-answer/route.ts
  - src/app/skeleton/ping/page.tsx
  - scripts/verify-rls.mjs
  - scripts/verify-dedup.mjs
autonomous: false
requirements: [RT-02, RT-05, SCOR-03, SCOR-04]

must_haves:
  truths:
    - "The deployed app returns HTTP 200 from the root route on the joc Vercel project"
    - "The root page performs a REAL read through the typed Supabase anon client (shows the seeded game phase + live question count from the cloud DB)"
    - "A real interaction (button -> server API route) performs a REAL write to the cloud DB and the second identical write is rejected by the DB UNIQUE constraint (23505)"
    - "The anon role cannot read questions.correct_option before reveal (verified with the anon key, not service role)"
    - "A post-build grep -r service_role .next/ returns no matches"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Root route reading seeded game state via the typed anon client (Walking Skeleton read)"
      contains: "createClient"
    - path: "src/app/api/skeleton-answer/route.ts"
      provides: "Server route inserting a test answer via service-role client; catches 23505 -> 409 (Walking Skeleton write + SCOR-03 proof)"
      contains: "23505"
    - path: "scripts/verify-rls.mjs"
      provides: "Anon-key test proving correct_option is not readable pre-reveal (D-12)"
      contains: "correct_option"
    - path: "scripts/verify-dedup.mjs"
      provides: "Duplicate-INSERT test asserting SQLSTATE 23505 (SCOR-03 / ROADMAP SC5)"
      contains: "23505"
  key_links:
    - from: "src/app/page.tsx"
      to: "cloud games/questions tables"
      via: "typed anon createClient().from('games'/'questions_public').select()"
      pattern: "from\\(['\"](games|questions_public)['\"]\\)"
    - from: "src/app/api/skeleton-answer/route.ts"
      to: "answers table UNIQUE constraint"
      via: "service-role insert catching error.code === '23505'"
      pattern: "23505"
    - from: "scripts/verify-rls.mjs"
      to: "anon-key read of correct_option"
      via: "anon client select expecting denial / absent column"
      pattern: "ANON"
---

## Phase Goal

**As a** wedding guest, **I want to** open the deployed app and have it show live data from the real database (and have my answer recorded exactly once), **so that** the full Next.js -> typed Supabase -> cloud Postgres -> RLS -> Vercel stack is proven end-to-end before any game feature is built.

<objective>
Close the Walking Skeleton: a real end-to-end slice that exercises the entire stack. The root page does a REAL typed-anon-client READ of the seeded game (phase + question count) from the cloud DB; a button triggers a server API route that does a REAL service-role WRITE to `answers` and proves the `UNIQUE(player_id, question_id)` anti-cheat by catching 23505 on a duplicate; two verification scripts prove (a) the anon role cannot read `questions.correct_option` (D-12), and (b) a duplicate answer INSERT is rejected with 23505 (SCOR-03 / ROADMAP SC5); a post-build grep confirms the service-role key is absent from `.next/` (ROADMAP SC4); and the app is deployed to the new `joc` Vercel project returning 200 (ROADMAP SC1, D-03).

Purpose: This is the riskiest-integration proof for the whole project — typed client read + real write + RLS secrecy + serverless deploy all working together. It also bakes in the concrete, testable acceptance criteria that stand in for the absent Nyquist VALIDATION.md. Completing it satisfies all five Phase-1 ROADMAP success criteria. Covers RT-02 (server write + the broadcast surface is import-reachable), RT-05 (deployed on the cloud project sized for 100+), SCOR-03 and SCOR-04 (dedup proven; phase field present and read-reachable).

Output: a working deployed skeleton page, a write API route, two verification scripts, a clean post-build grep, and a live `joc` Vercel deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-schema/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-schema/01-01-SUMMARY.md
@.planning/phases/01-foundation-schema/01-02-SUMMARY.md
@src/lib/supabase/client.ts
@src/lib/supabase/server.ts
@src/lib/supabase/admin.ts
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skeleton read page + real-write API route (Walking Skeleton happy path)</name>
  <files>src/app/page.tsx, src/app/skeleton/ping/page.tsx, src/app/api/skeleton-answer/route.ts</files>
  <read_first>
    - src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/supabase/admin.ts (typed clients from Plans 01-02)
    - src/types/database.ts (generated types — table shapes)
    - .planning/research/ARCHITECTURE.md (§"Answer Deduplication (Two Layers)" — the 23505 -> 409 pattern; §"Phase Guard Before Answer Insert" — reading games.phase before insert)
    - supabase/seed.sql (the seeded game id + question rows the page reads)
    - .planning/phases/01-foundation-schema/01-CONTEXT.md (D-12, D-13)
  </read_first>
  <action>
    Root page `src/app/page.tsx` (Walking Skeleton READ): a Server Component (or a small client component using the anon `createClient()`) that reads, via the TYPED ANON client, the single seeded `games` row (its `phase`) and a COUNT of rows in the `questions_public` view (NOT base questions — proving anon reads go through the secrecy view). Render a minimal soft-luxury-themed card (using the D-16 theme tokens / `.glass` placeholder) showing e.g. "Joc — phase: lobby — N questions loaded". This is a real cloud read on first paint; do not hardcode the values.
    `src/app/skeleton/ping/page.tsx` (Walking Skeleton INTERACTION): a tiny client page with one button "Record test answer" that POSTs to `/api/skeleton-answer`. Show the JSON result (200 first time, 409 on the repeat) so a human can see the dedup live. This is a throwaway proof harness for Phase 1 only — note in a comment it is removed/replaced by the real guest flow in Phase 5.
    `src/app/api/skeleton-answer/route.ts` (Walking Skeleton WRITE + SCOR-03/SCOR-04 substrate): a Route Handler (POST) using the SERVER service-role client. It must: (1) read the seeded game's `phase` from `games` (the SCOR-04 phase-guard substrate — for the skeleton, accept the write only if phase is 'lobby' or 'question' to demonstrate the guard reads the field; return 403 otherwise); (2) ensure a fixed skeleton `players` row exists for a constant device_token (upsert on (game_id, device_token) — exercises the idempotent-join UNIQUE); (3) INSERT into `answers` a row for (that player_id, the first seeded question_id, choice 'A'); (4) if the insert errors with `error.code === '23505'` return 409 `{error:'already_answered'}`; on success return 200. This route is the live SCOR-03 demonstration: calling it twice yields 200 then 409 without a second DB row. Per D-13 the route imports only the server client; never the anon key for the privileged write. Per D-04 no git commit.
  </action>
  <verify>
    <automated>grep -E "from\(['\"](games|questions_public)['\"]\)" src/app/page.tsx; grep -c "23505" src/app/api/skeleton-answer/route.ts; grep -c "phase" src/app/api/skeleton-answer/route.ts; npm run build 2>&1 | tail -5; npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` reads `games` and `questions_public` through the typed anon client (no hardcoded phase/count)
    - `src/app/api/skeleton-answer/route.ts` reads `games.phase` before inserting (SCOR-04 substrate), upserts a player on (game_id, device_token), inserts an answer, and returns 409 with `error.code === '23505'` on duplicate
    - The route imports the server/service-role client only (no anon key, no NEXT_PUBLIC_ service usage)
    - `npm run build` and `npx tsc --noEmit` both pass
    - `src/app/skeleton/ping/page.tsx` posts to `/api/skeleton-answer` and displays the response
  </acceptance_criteria>
  <done>Opening the app shows live seeded data from the cloud DB; clicking the skeleton button records an answer once and is rejected (409) on repeat — the full read+write stack works locally.</done>
</task>

<task type="auto">
  <name>Task 2: Anon-RLS secrecy test + duplicate-INSERT 23505 test + post-build key grep</name>
  <files>scripts/verify-rls.mjs, scripts/verify-dedup.mjs</files>
  <read_first>
    - .planning/research/PITFALLS.md (#9 service_role grep verification; #11 test RLS with the ANON key not service role; "Looks Done But Isn't" checklist)
    - .planning/research/ARCHITECTURE.md (§"Database Schema" constraints — what the dedup test asserts)
    - supabase/migrations/0002_rls_policies.sql (the questions_public view + base-questions anon denial — what verify-rls asserts)
    - .planning/phases/01-foundation-schema/01-CONTEXT.md (D-12, D-13)
  </read_first>
  <action>
    `scripts/verify-rls.mjs` (D-12, ROADMAP SC3): a Node script that creates a Supabase client with the ANON key (read `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` from env). Assert: (1) `select('correct_option')` from base `questions` as anon returns either an error/permission-denied OR zero readable rows (i.e. anon cannot read correct_option); (2) `select('*')` from `questions_public` as anon SUCCEEDS and the returned objects do NOT have a `correct_option` key. Print PASS/FAIL per assertion; exit non-zero on any FAIL. This is the SC3 "tested with the anon key (not service role)" requirement.
    `scripts/verify-dedup.mjs` (SCOR-03, ROADMAP SC5): a Node script using the SERVICE-ROLE key (so it can write directly, simulating the DB layer). Ensure a throwaway player exists for a test device_token; INSERT an answer for (player_id, a seeded question_id, 'A') — expect success; INSERT the SAME (player_id, question_id) again — assert the error has `code === '23505'` (unique violation). Clean up the test rows afterward (or use the reset_game function). Print PASS/FAIL; exit non-zero unless the second insert raised 23505.
    Post-build key isolation (ROADMAP SC4, PITFALLS #9): the executor runs `npm run build` then `grep -r "service_role" .next/` and confirms NO matches (D-13: this is a one-time verification, not an automated build hook — user opted out of the hook). Document the command + result in the SUMMARY. (Do not add it to package.json scripts.)
    Add `scripts/verify-rls.mjs` and `scripts/verify-dedup.mjs` runnable via `node scripts/verify-rls.mjs` / `node scripts/verify-dedup.mjs`. Per D-04 no git commit.
  </action>
  <verify>
    <automated>node scripts/verify-rls.mjs; node scripts/verify-dedup.mjs; npm run build > /dev/null 2>&1 && (grep -rl "service_role" .next/ && echo "FAIL_KEY_IN_BUNDLE" || echo "PASS_NO_SERVICE_ROLE_IN_BUILD")</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-rls.mjs` exits 0: anon cannot read `correct_option` from base `questions`, and `questions_public` rows have no `correct_option` key
    - `node scripts/verify-dedup.mjs` exits 0: the second INSERT of the same (player_id, question_id) fails with SQLSTATE `23505`
    - After `npm run build`, `grep -r "service_role" .next/` returns NO matches (prints PASS_NO_SERVICE_ROLE_IN_BUILD)
    - Both scripts use the correct key (verify-rls: anon; verify-dedup: service role) per PITFALLS #11
  </acceptance_criteria>
  <done>Automated proofs exist and pass for the two security/anti-cheat invariants (answer secrecy + duplicate rejection), and the production build is confirmed free of the service-role key.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (checkpoint): Deploy to the joc Vercel project and verify 200</name>
  <action>Human-gated: user creates the new joc Vercel project (Production Branch = joc, isolated domain, D-03), sets the four env vars (service-role + HOST_PASSWORD as non-public), and deploys; verify curl -I https://<url>/ returns 200 and the page shows live data. See how-to-verify for steps. Per D-04 no git commit.</action>
  <what-built>The skeleton page, write route, and verification scripts pass locally (Tasks 1-2). The final Walking-Skeleton requirement is a live deployment: a NEW, separate Vercel project with Production Branch = `joc` (D-03), returning 200 from root. Creating the Vercel project, connecting the repo/branch, and setting env vars require the user's Vercel dashboard access; the assistant cannot self-provision the project.</what-built>
  <how-to-verify>
    1. In the Vercel dashboard, create a NEW project (separate from any project serving `master`), import this repo, set Production Branch = `joc`, and assign an isolated domain (D-03).
    2. Add env vars in the Vercel project settings: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `HOST_PASSWORD` (service-role + HOST_PASSWORD as plain server env vars, NOT NEXT_PUBLIC_ — D-13).
    3. Trigger a deploy of the `joc` branch (the executor can run `npx vercel --prod` if the Vercel CLI is authenticated, or the user deploys from the dashboard).
    4. Verify: `curl -I https://<your-joc-deployment>/` returns `200`, and the deployed root page shows the live seeded game phase + question count (proving the deployed app reads the cloud DB).
  </how-to-verify>
  <resume-signal>Type "deployed" with the deployment URL once `curl -I https://<url>/` returns 200 and the page shows live data, or paste the Vercel build/deploy error to debug.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deployed client bundle -> Supabase | Only anon key reaches the browser; service-role key must be absent from `.next/`. |
| Guest POST -> /api/skeleton-answer | Untrusted client input crosses into a privileged service-role write path; phase guard + DB constraints are the controls. |
| Vercel env vars -> runtime | service-role + HOST_PASSWORD set as non-public env vars in the joc project only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Information Disclosure | service_role key shipped in the deployed client bundle | mitigate | Post-build `grep -r service_role .next/` returns no matches (Task 2); service-role read confined to the server route + server modules (server-only, Plan 01). ROADMAP SC4. (PITFALLS #9) |
| T-01-11 | Information Disclosure | Deployed page leaking correct_option via the read path | mitigate | Root page reads `questions_public` (no correct_option column); `verify-rls.mjs` asserts anon cannot read base `questions.correct_option`. (D-12) |
| T-01-12 | Tampering | Replayed/duplicate answer via the public API route | mitigate | DB `UNIQUE (player_id, question_id)` -> 23505 -> 409; `verify-dedup.mjs` asserts the DB rejects the second insert independent of API logic. (SCOR-03, PITFALLS #5) |
| T-01-13 | Spoofing | Forged write while answers are locked | mitigate (substrate) | Route reads `games.phase` before insert and rejects non-open phases (403) — the SCOR-04 phase-guard substrate; full host-driven lock flow lands in Phase 3. |
| T-01-14 | Information Disclosure | Wrong env-var scoping on Vercel (service key as NEXT_PUBLIC_) | mitigate | Deploy checkpoint instructs setting service-role + HOST_PASSWORD as non-public env vars only; post-build grep would catch a NEXT_PUBLIC_ leak. (D-13) |
</threat_model>

<package_legitimacy>
No new package-manager installs in this plan (uses deps installed in Plan 01; Vercel CLI is invoked via npx if the user authenticates). No legitimacy checkpoint required.
</package_legitimacy>

<verification>
- Deployed app returns 200 from root on the joc Vercel project (ROADMAP SC1).
- Root page reads live seeded data via the typed anon client (read proven against cloud DB).
- `/api/skeleton-answer` returns 200 then 409 on duplicate (SCOR-03 live; SCOR-04 phase-guard substrate exercised).
- `verify-rls.mjs` passes: anon cannot read correct_option (ROADMAP SC3).
- `verify-dedup.mjs` passes: second insert raises 23505 (ROADMAP SC5).
- Post-build `grep -r service_role .next/` clean (ROADMAP SC4).
</verification>

<success_criteria>
All five Phase-1 ROADMAP success criteria are demonstrably TRUE:
1. App deploys to Vercel on `joc` and returns 200 from root.
2. Five tables with correct columns/CHECK/UNIQUE exist (from Plan 02; the page + scripts exercise them live).
3. RLS enabled; `correct_option` not anon-readable, tested with the anon key.
4. service-role key present only server-side; post-build grep of `.next/` clean.
5. Duplicate answer INSERT rejected with 23505.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-schema/01-03-SUMMARY.md` when done.
</output>
