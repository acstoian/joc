---
phase: 04-host-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/host/PhaseButton.tsx
  - src/components/host/ControlTab.tsx
autonomous: false
requirements: [HOST-11]
must_haves:
  truths:
    - "Phase control buttons (Porneste / Blocheaza / Dezvaluie / Urmatoarea / Incheie) are enabled only when valid for the current phase (D-05, SC4)"
    - "Clicking a button disables ALL phase buttons while the request is in-flight (no double-tap can advance two steps) (SC4)"
    - "Buttons re-enable when the Broadcast confirms the new phase via useGameSync state.phase — NOT in the fetch success handler (RQ-6, Pitfall 2)"
    - "A 409 CAS conflict, 4xx, or 5xx re-enables buttons immediately and shows a Romanian toast (RQ-6)"
    - "The Control tab shows the current phase badge, participant count, and a live A/B distribution bar (SC4 status strip)"
  artifacts:
    - path: "src/components/host/PhaseButton.tsx"
      provides: "Single phase-action button with enabled/disabled/in-flight states"
      exports: ["PhaseButton"]
    - path: "src/components/host/ControlTab.tsx"
      provides: "Phase control surface driving Phase 3 transition/reveal routes"
      exports: ["ControlTab"]
  key_links:
    - from: "src/components/host/ControlTab.tsx"
      to: "/api/host/transition"
      via: "hostFetch POST with x-host-password"
      pattern: "api/host/transition"
    - from: "src/components/host/ControlTab.tsx"
      to: "/api/host/reveal"
      via: "hostFetch POST"
      pattern: "api/host/reveal"
    - from: "src/components/host/ControlTab.tsx"
      to: "useGameSync state.phase"
      via: "useEffect re-enable on phase change"
      pattern: "state\\?\\.phase"
---

<objective>
Replace the Control tab placeholder with the live phase-control surface (D-05, SC4). The host drives the existing Phase 3 routes — `POST /api/host/transition` (start/lock/next/end), `POST /api/host/reveal`, — with the in-flight disable pattern: clicking a button disables all buttons, and they re-enable only when the Broadcast-confirmed `state.phase` updates (not on the fetch response). Errors (409 CAS / 4xx / 5xx / network) re-enable immediately with a Romanian toast.

Purpose: This is the host's primary live surface. Correct in-flight behavior prevents double-advancing the game on a slow phone (SC4) — the single highest-risk interaction.
Output: `PhaseButton` component, fully-wired `ControlTab` with status strip (phase badge + participant count + A/B bar). Emergency controls are added in Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-host-dashboard/04-RESEARCH.md
@.planning/phases/04-host-dashboard/04-PATTERNS.md
@.planning/phases/04-host-dashboard/04-UI-SPEC.md
@.planning/phases/04-host-dashboard/04-01-SUMMARY.md
@src/app/api/host/transition/route.ts
@src/app/api/host/reveal/route.ts
</context>

## Phase Goal (user story)

**As a** wedding host, **I want to** tap Start, Lock, Reveal, Next, and End and have each button enabled only when valid and locked while the action is confirming, **so that** I never accidentally skip or double-advance a question in front of the room.

<tasks>

<task type="auto">
  <name>Task 1: PhaseButton component (enabled / disabled / in-flight)</name>
  <files>src/components/host/PhaseButton.tsx</files>
  <read_first>
    - src/app/sync-demo/page.tsx (button class pattern lines 119-121, aria-busy/disabled lines 162-171)
    - src/components/ui/button.tsx (the shadcn Button just installed)
    - .planning/phases/04-host-dashboard/04-UI-SPEC.md (§5.3 phase buttons sizing min-h-[56px], §6 interaction states, §10 aria-busy/aria-disabled)
    - .planning/phases/04-host-dashboard/04-PATTERNS.md (PhaseButton pattern, Touch Target + Color Tokens shared patterns)
  </read_first>
  <action>
    Create `src/components/host/PhaseButton.tsx` ("use client"). Props: `label` (Romanian string), `enabled` (valid for current phase), `inFlight` (this action is the one currently pending), `anyInFlight` (some action is pending), `onClick`. Render a `min-h-[56px]` full-width button (UI-SPEC §5.3). Disabled when `!enabled || anyInFlight`. Use `aria-disabled={!enabled}` (so screen readers announce the phase reason — §10) and `aria-busy={inFlight}`. When `inFlight`, show the lucide `Loader2` icon with `animate-spin` plus the label suffix "...". Valid/active styling: `bg-gold/20 text-gold-bright border border-gold/40 hover:bg-gold/30`; disabled styling: `opacity-40 cursor-not-allowed`. Use `motion` from `motion/react` for the subtle press-confirm `animate={{ scale: [1, 0.96, 1] }}` (200ms) per UI-SPEC §9 — import from `motion/react`, never framer-motion. Use only @theme color tokens, no raw hex.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/host/PhaseButton.tsx exports PhaseButton, starts with "use client"
    - Button is min-h-[56px], sets aria-busy when inFlight and aria-disabled when not enabled
    - Imports Loader2 from "lucide-react" and motion from "motion/react" (NOT "framer-motion")
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PhaseButton renders enabled/disabled/in-flight states with correct aria attributes; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: ControlTab — status strip + phase machine wiring with Broadcast-confirmed re-enable</name>
  <files>src/components/host/ControlTab.tsx</files>
  <read_first>
    - src/app/api/host/transition/route.ts (request body {gameId, action, nextQuestionId?}; actions start/lock/next/end; 409 invalid_transition; 200 noop; end expectedFrom=revealed ONLY)
    - src/app/api/host/reveal/route.ts (body {gameId, choice:"A"|"B"}; 409 invalid_transition; the host picks correct option AT reveal time)
    - src/hooks/useGameSync.ts (state.phase, state.distribution, status, participantCount; phase values lobby/question/locked/revealed/ended)
    - src/lib/host/constants.ts (GAME_ID, hostFetch)
    - src/components/ui/sonner.tsx (toast() API for feedback)
    - src/app/sync-demo/page.tsx (SubscriberPane status colors lines 46-54, participant display lines 71-74, glass card)
    - .planning/phases/04-host-dashboard/04-RESEARCH.md (RQ-6 in-flight pattern + Pattern 3 PHASE_ACTIONS map; Open Q1: End enabled only in revealed)
    - .planning/phases/04-host-dashboard/04-PATTERNS.md (ControlTab pattern: PHASE_ACTIONS, re-enable useEffect, 5s timeout)
    - .planning/phases/04-host-dashboard/04-UI-SPEC.md (§5.3 control tab layout + button table + Romanian labels, §7 error toasts, §9 distribution bar)
  </read_first>
  <action>
    Replace `src/components/host/ControlTab.tsx` ("use client"). Accept the shared props from Plan 01 (`state`, `status`, `participantCount`, `password`, `gameId`). Section A — status strip (always visible): a phase `Badge` mapping `state.phase` to Romanian labels ("In asteptare"/lobby, "Intrebare"/question, "Blocat"/locked, "Dezvaluit"/revealed, "Incheiat"/ended) with semantic colors (sage idle, blush locked, gold-bright revealed per UI-SPEC §4); the participant count as `text-3xl font-bold text-gold-bright` + "jucatori conectati"; and a live A/B distribution bar built from `state.distribution` ({A,B} counts; show "Niciun raspuns inca." when null). Section B — a 2-column grid of `PhaseButton`s per UI-SPEC §5.3 table: "Porneste Jocul" (start, enabled in lobby), "Blocheaza Raspunsurile" (lock, question), "Dezvaluie Raspunsul" (reveal, locked), "Urmatoarea Intrebare" (next, revealed), "Incheie Jocul" (end, revealed ONLY — per Open Q1 the transition route's end is expectedFrom=revealed; force-end-from-any-state is the Emergency panel's job in Plan 05). Build the `PHASE_ACTIONS` map (lobby:{start}, question:{lock}, locked:{reveal}, revealed:{next,end}, ended:{}) and `isActionEnabled(action, state?.phase)`. Hold a single `inFlight: string | null` state. On click: if `inFlight !== null` return (block double-tap); set `inFlight = action`; call hostFetch — start/lock/end via `POST /api/host/transition` with body `{ gameId, action }`; reveal via `POST /api/host/reveal` with body `{ gameId, choice }` (the host must pick the correct A/B at reveal — surface a small A/B picker per sync-demo pattern, defaulting to whatever the host selected). "Urmatoarea Intrebare" needs `nextQuestionId`; in this plan, advance to the next question by display_order — since ControlTab does not own the question list, resolve `nextQuestionId` by reading it from props if available, otherwise leave the "next" action wired to send the question list's next id (the question list is owned by Plan 03; for this plan, if no nextQuestionId is resolvable, the next button may call transition with the current logic and the executor should document the resolution path; do not fabricate a v1 stub — wire it to the real next-question id source available via the questions data passed through props or fetched via GET /api/host/questions). On non-ok response: read status; show toast — 409 → "Starea jocului s-a schimbat. Actiunea nu mai este valida.", other 4xx → "Actiunea a esuat. Verifica conexiunea si incearca din nou.", 5xx → "Eroare de server. Incearca din nou in cateva secunde."; then `setInFlight(null)` immediately. On ok response: do NOT setInFlight(null) here. Re-enable via `useEffect(() => { if (inFlight !== null) setInFlight(null) }, [state?.phase])` (Broadcast-confirmed) plus a 5s fallback `useEffect` timeout (RQ-6, Pitfall 2). Use only @theme tokens; all copy Romanian.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ControlTab re-enables buttons in a `useEffect(..., [state?.phase])`, not inside the fetch `.then`/success branch (grep: `[state?.phase]` dependency present; setInFlight(null) appears in the phase-effect)
    - A 5-second fallback setTimeout that clears inFlight exists
    - "Incheie Jocul" (end) is enabled only when phase === "revealed" (PHASE_ACTIONS.revealed contains "end"; PHASE_ACTIONS.question does NOT)
    - transition calls send `{ gameId, action }` and reveal sends `{ gameId, choice }` via hostFetch with x-host-password
    - 409 path shows the "Starea jocului s-a schimbat..." toast and re-enables immediately
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>ControlTab drives all Phase 3 control routes with correct phase-gated enable, in-flight lock, Broadcast-confirmed re-enable, and Romanian error toasts; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The live Control tab: phase status strip (badge + participant count + A/B bar) and the five phase-control buttons wired to the Phase 3 transition/reveal routes with in-flight locking.</what-built>
  <how-to-verify>
    1. Ensure HOST_PASSWORD is set and the seed game exists with at least one question (add via Prisma/Supabase studio or wait for Plan 03). Run `npm run dev`, log into /host.
    2. On the Control tab, confirm only "Porneste Jocul" is enabled in lobby; the rest are dimmed (opacity-40).
    3. Tap "Porneste Jocul" — it should disable immediately (spinner + "..."), and ALL buttons stay disabled until the phase badge changes to "Intrebare", at which point "Blocheaza Raspunsurile" becomes enabled. Time from tap to re-enable should be ~1s (Broadcast).
    4. Rapidly double-tap a valid button — the game must advance by exactly one step (no double-advance).
    5. Open a second tab also on /host; trigger a phase change from tab A and confirm tab B's badge + button states update within ~2s.
    6. Force a 409 (e.g. tap a button twice fast across two tabs) — expect the "Starea jocului s-a schimbat..." toast and buttons reflecting the real phase.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the incorrect behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Control tab → /api/host/* | Client sends x-host-password; server re-validates every request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | host transition/reveal calls | mitigate | All calls go through hostFetch which attaches x-host-password; routes already call validateHostAuth first (Phase 3). No new server surface added. |
| T-04-05 | Tampering | double-tap race advancing two phases | mitigate | Single inFlight guard blocks concurrent clicks; server CAS (D-07) returns 409/noop for lost races; UI re-enables on Broadcast-confirmed phase only (SC4). |
| T-04-06 | Denial of Service | Broadcast never arrives, buttons stuck | mitigate | 5-second fallback timeout clears inFlight so the dashboard never permanently locks (RQ-6). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds
- grep: re-enable lives in `useEffect([state?.phase])`, 5s timeout present
- grep: PHASE_ACTIONS.revealed includes "end", PHASE_ACTIONS.question does not
- Manual: double-tap advances exactly one step; 409 toast shows
</verification>

<success_criteria>
- SC4 satisfied: phase buttons enabled only when valid; in-flight disables until Broadcast confirms
- HOST-11 partially served (normal End from revealed); emergency force-end is Plan 05
</success_criteria>

## Artifacts this phase produces (created by this plan)

- Component `PhaseButton` (`src/components/host/PhaseButton.tsx`) — props: label, enabled, inFlight, anyInFlight, onClick
- Component `ControlTab` (`src/components/host/ControlTab.tsx`) — replaces Plan 01 placeholder; consumes shared props {state, status, participantCount, password, gameId}
- Local `PHASE_ACTIONS` map + `isActionEnabled(action, phase)` helper (lobby→start, question→lock, locked→reveal, revealed→next+end)
- Consumes existing routes: `POST /api/host/transition` `{gameId, action, nextQuestionId?}`, `POST /api/host/reveal` `{gameId, choice}`

<output>
Create `.planning/phases/04-host-dashboard/04-02-SUMMARY.md` when done
</output>
