---
phase: 06-tv-display-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/hooks/useGameSync.ts
  - src/app/api/host/countdown/route.ts
  - src/app/display/page.tsx
  - src/components/display/LoadingDisplay.tsx
  - src/components/display/LobbyDisplay.tsx
  - src/components/display/DisplayStatusDot.tsx
  - src/components/display/QuestionDisplay.tsx
  - src/components/display/LockedDisplay.tsx
  - src/components/display/RevealDisplay.tsx
  - src/components/display/WinnerDisplay.tsx
  - src/components/display/CountdownOverlay.tsx
autonomous: false
requirements: [DISP-01, DISP-02]
user_setup: []

must_haves:
  truths:
    - "Opening /display in a browser shows a full-screen landscape layout on bg-ink (DISP-01)"
    - "The /display route renders independently with no host dashboard open, subscribing to the live game channel (DISP-02)"
    - "When state.phase is 'lobby', the display shows the game title and a live participant count that pulses when it changes (D-01)"
    - "A persistent connection status dot is visible top-right: green when connected, amber pulsing when reconnecting (D-11, DISP-02)"
    - "An 'Ecran complet' button enters browser fullscreen on the root container and hides itself after entering (D-10)"
    - "useGameSync accepts an optional third onEvent argument; existing host and guest callers are unaffected (Finding 2)"
    - "POST /api/host/countdown rejects unauthenticated requests with 401 and clamps seconds to 1-10 (Finding 6)"
    - "npm run build and npm run lint both exit 0"
  artifacts:
    - path: "src/app/display/page.tsx"
      provides: "DisplayPage shell — useGameSync(GAME_ID, HOST_SENTINEL_PLAYER_ID), phase switch, countdown state, fullscreen button"
      exports: ["default"]
      min_lines: 80
    - path: "src/components/display/LobbyDisplay.tsx"
      provides: "Lobby screen — game title + pulsing participant count"
      exports: ["LobbyDisplay"]
    - path: "src/components/display/DisplayStatusDot.tsx"
      provides: "Fixed connection status dot driven by SyncStatus"
      exports: ["DisplayStatusDot"]
    - path: "src/components/display/LoadingDisplay.tsx"
      provides: "Pre-sync loading spinner"
      exports: ["LoadingDisplay"]
    - path: "src/app/api/host/countdown/route.ts"
      provides: "POST endpoint — validateHostAuth + seconds clamp + COUNTDOWN_STARTED broadcast"
      exports: ["POST"]
    - path: "src/hooks/useGameSync.ts"
      provides: "onEvent callback extension (optional third argument)"
      contains: "options?:"
    - path: "src/app/globals.css"
      provides: "slide-up + fade-scale keyframes + reduced-motion overrides"
      contains: "@keyframes slide-up"
  key_links:
    - from: "src/app/display/page.tsx"
      to: "src/hooks/useGameSync.ts"
      via: "useGameSync(GAME_ID, HOST_SENTINEL_PLAYER_ID, { onEvent })"
      pattern: "useGameSync\\(\\s*GAME_ID"
    - from: "src/app/api/host/countdown/route.ts"
      to: "src/lib/auth/host.ts"
      via: "validateHostAuth(req) as first statement"
      pattern: "validateHostAuth\\(req\\)"
    - from: "src/app/display/page.tsx"
      to: "src/components/display/DisplayStatusDot.tsx"
      via: "renders <DisplayStatusDot status={status} />"
      pattern: "DisplayStatusDot"
---

<objective>
Establish the TV Display Mode foundation and ship the first end-to-end vertical slice: open `/display` and see a live, auto-syncing lobby on a full-screen landscape canvas with a connection status dot and a fullscreen button.

This plan also lands all Phase 6 Wave-0 infrastructure so Waves 2 (plans 06-02, 06-03) can build screens in parallel without touching shared files:
- `globals.css` keyframes (`slide-up`, `fade-scale`) — Finding 4
- `useGameSync` optional `onEvent` callback — Finding 2
- `POST /api/host/countdown` endpoint — Finding 6
- `src/app/display/page.tsx` shell with the COMPLETE phase switch and all screen imports wired
- All eight `src/components/display/*` files as either real (this plan) or compiling stubs (replaced by 06-02/06-03)

Purpose: A pure-subscriber TV surface that is provably live before any game UI is layered on. Mirrors the Phase 5 GuestShell pattern (single `useGameSync`, props-down screens) but anonymous (no NameGate, no identity) — uses `HOST_SENTINEL_PLAYER_ID` exactly like the host dashboard.

Output: `/display` route renders the lobby with a live participant count and connection dot (DISP-01, DISP-02); the keyframes, hook extension, and countdown endpoint are in place for 06-02 and 06-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tv-display-mode/06-CONTEXT.md
@.planning/phases/06-tv-display-mode/06-RESEARCH.md
@.planning/phases/06-tv-display-mode/06-UI-SPEC.md

# Patterns to replicate (read before implementing)
@src/app/page.tsx
@src/app/host/page.tsx
@src/hooks/useGameSync.ts
@src/lib/host/constants.ts
@src/lib/realtime/events.ts
@src/app/api/host/transition/route.ts
@src/lib/auth/host.ts
@src/lib/supabase/admin.ts
@src/app/globals.css
</context>

<artifacts_this_phase_produces>
Symbols and files created in Plan 06-01 (consumed by 06-02, 06-03, and the running app):

| Symbol / File | Kind | Consumed by |
|---------------|------|-------------|
| `useGameSync(gameId, playerId, options?)` `options.onEvent?: (event: GameEvent) => void` | hook signature extension | DisplayPage (06-01), any future event consumer |
| `POST /api/host/countdown` | route handler | ControlTab countdown button (06-03) |
| `@keyframes slide-up` (globals.css) | CSS animation | QuestionDisplay / LockedDisplay (06-02) |
| `@keyframes fade-scale` (globals.css) | CSS animation | CountdownOverlay (06-03), reveal effects |
| `default` export `DisplayPage` (`src/app/display/page.tsx`) | page | Next.js route `/display` |
| `LobbyDisplay` | component | DisplayPage |
| `LoadingDisplay` | component | DisplayPage |
| `DisplayStatusDot` | component | DisplayPage |
| `QuestionDisplay` (stub) | component | DisplayPage; replaced by 06-02 |
| `LockedDisplay` (stub) | component | DisplayPage; replaced by 06-02 |
| `RevealDisplay` (stub) | component | DisplayPage; replaced by 06-02 |
| `WinnerDisplay` (stub) | component | DisplayPage; replaced by 06-03 |
| `CountdownOverlay` (stub) | component | DisplayPage; replaced by 06-03 |
</artifacts_this_phase_produces>

<critical_corrections>
The 06-CONTEXT.md and 06-UI-SPEC.md contain THREE factual errors that this plan corrects. Executors MUST follow these corrections, not the upstream docs:

1. **`useGameSync` does NOT accept `null`.** The signature is `(gameId: string, playerId: string)`. Pass `HOST_SENTINEL_PLAYER_ID` (already exported from `@/lib/host/constants`), exactly like `src/app/host/page.tsx` does. Passing `null` is a TS2345 compile error. (Finding 1)

2. **`GameStateSnapshot.currentQuestion` field is `body`, NOT `text`.** The real shape (from `src/hooks/useGameSync.ts`) is `{ id, body, optionA, optionB }`. The UI-SPEC says `state.currentQuestion.text` — that field does NOT exist. Use `state.currentQuestion.body`. (Verified against useGameSync.ts lines 47-52)

3. **There is NO `questionIndex` / question-number field in `GameStateSnapshot`.** The UI-SPEC's "Întrebarea {N}" label cannot read an index. Use the static label `"Întrebarea"` (matching `src/components/guest/QuestionScreen.tsx` line 124-126) — do NOT invent an index. (Verified against GameStateSnapshot type)

`leaderboard` entries are `{ name: string; score: number }` — already correct in UI-SPEC.
</critical_corrections>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 infrastructure — keyframes, useGameSync onEvent callback, countdown endpoint</name>
  <files>src/app/globals.css, src/hooks/useGameSync.ts, src/app/api/host/countdown/route.ts</files>
  <read_first>
    - src/app/globals.css (append keyframes after the .thin-divider block; do not alter @theme)
    - src/hooks/useGameSync.ts (broadcast handler at lines 180-186; signature at lines 68-71)
    - src/app/api/host/transition/route.ts (route pattern: validateHostAuth first, broadcast best-effort)
    - src/lib/auth/host.ts (validateHostAuth signature)
    - src/lib/supabase/admin.ts (broadcast(topic, event, payload) signature)
    - src/lib/realtime/events.ts (COUNTDOWN_STARTED member: { type, gameId, seconds })
  </read_first>
  <action>
    Three edits — pure infra, no UI yet.

    (a) globals.css: append AFTER the `.thin-divider` block (do NOT touch the `@theme` block):
    - `@keyframes slide-up` — from `{ opacity: 0; transform: translateY(2vh); }` to `{ opacity: 1; transform: translateY(0); }`
    - `@keyframes fade-scale` — from `{ opacity: 0.4; transform: scale(0.8); }` to `{ opacity: 1; transform: scale(1); }`
    - A `@media (prefers-reduced-motion: reduce)` block that redefines both keyframes to no-op (both from/to `{ opacity: 1; transform: none; }`) and adds `.animate-pulse { animation: none; }`.

    (b) useGameSync.ts: add an optional third parameter `options?: { onEvent?: (event: GameEvent) => void }` to the `useGameSync` function signature (after `playerId: string`). Inside the broadcast handler (currently `const _event = payload as GameEvent; void _event;`), change to call the onEvent callback BEFORE `await fetchState()`. Keep the existing re-fetch behavior intact — onEvent fires first, then fetchState runs. The handler must remain async and still await fetchState. To avoid stale-closure bugs and to keep the [gameId, playerId] dependency array unchanged, capture onEvent in a ref: create `const onEventRef = useRef(options?.onEvent)` at the top of the hook, update it in a `useEffect(() => { onEventRef.current = options?.onEvent; })` (no dependency array — runs every render), then call `onEventRef.current?.(event)` in the broadcast handler. Do NOT add onEvent to the main effect's dependency array — this preserves the existing reconnect logic exactly.

    (c) Create `src/app/api/host/countdown/route.ts` following the transition/route.ts pattern EXACTLY:
    - `export async function POST(req: NextRequest): Promise<NextResponse>`
    - FIRST statement: `if (!validateHostAuth(req)) return NextResponse.json({ error: "unauthorized" }, { status: 401 });`
    - Parse body in try/catch → 400 `invalid_json` on failure.
    - Destructure `{ gameId, seconds }`. Validate `gameId` with the UUID_REGEX pattern (copy the `isValidUuid` helper from transition/route.ts) → 400 `gameId required` if invalid.
    - Validate seconds: `if (typeof seconds !== "number" || !Number.isInteger(seconds) || seconds < 1 || seconds > 10) return NextResponse.json({ error: "seconds must be an integer 1-10" }, { status: 400 });` (Finding 6 / security clamp — reject, do not silently clamp, so a buggy caller is visible).
    - Best-effort broadcast: `await broadcast(\`game:${gameId}\`, "GAME_EVENT", { type: "COUNTDOWN_STARTED", gameId, seconds } satisfies GameEvent as Record<string, unknown>)` wrapped in try/catch that logs and proceeds (cosmetic signal — never fail the request on broadcast error, per transition route precedent).
    - Return `NextResponse.json({ ok: true }, { status: 200 })`.
    - Imports: `NextRequest, NextResponse` from next/server; `broadcast` from `@/lib/supabase/admin`; `validateHostAuth` from `@/lib/auth/host`; `GameEvent` type from `@/lib/realtime/events`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains `@keyframes slide-up` and `@keyframes fade-scale` and a `prefers-reduced-motion` override block
    - `useGameSync` signature has an optional third argument typed `{ onEvent?: (event: GameEvent) => void }`
    - The useGameSync broadcast handler calls the onEvent callback (via a ref) before `await fetchState()`
    - `src/app/host/page.tsx` and `src/app/page.tsx` still compile unchanged (they pass no third argument)
    - `src/app/api/host/countdown/route.ts` exists and exports `POST`
    - `validateHostAuth(req)` is the first executable statement in the POST handler
    - The handler returns 400 when `seconds` is not an integer in 1-10
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Keyframes, onEvent callback, and the host-auth-gated countdown endpoint with a 1-10 seconds clamp all exist; build is green; no existing useGameSync caller is broken.</done>
</task>

<task type="auto">
  <name>Task 2: Display shell + all screen files (real LoadingDisplay/LobbyDisplay/DisplayStatusDot; stubs for the rest)</name>
  <files>src/app/display/page.tsx, src/components/display/LoadingDisplay.tsx, src/components/display/LobbyDisplay.tsx, src/components/display/DisplayStatusDot.tsx, src/components/display/QuestionDisplay.tsx, src/components/display/LockedDisplay.tsx, src/components/display/RevealDisplay.tsx, src/components/display/WinnerDisplay.tsx, src/components/display/CountdownOverlay.tsx</files>
  <read_first>
    - src/app/page.tsx (GuestShell phase-switch pattern — single useGameSync, props-down, null-state handling, exhaustive default with `never`)
    - src/app/host/page.tsx (DashboardShell — useGameSync(GAME_ID, HOST_SENTINEL_PLAYER_ID))
    - src/hooks/useGameSync.ts (GameStateSnapshot + SyncStatus types; onEvent from Task 1)
    - src/lib/host/constants.ts (GAME_ID, HOST_SENTINEL_PLAYER_ID)
    - src/lib/realtime/events.ts (GameEvent — COUNTDOWN_STARTED discriminant)
    - src/lib/utils.ts (cn helper)
    - src/components/guest/SyncStatusBadge.tsx (status-driven rendering pattern to adapt for the dot)
    - .planning/phases/06-tv-display-mode/06-UI-SPEC.md (§DisplayPage Shell, §LobbyDisplay, §LoadingDisplay, §DisplayStatusDot — typography + token classes)
  </read_first>
  <action>
    Create the display route shell and the screen components. The shell's phase switch references ALL eight screen components, so create every file now — three real, five as compiling stubs that 06-02/06-03 replace (the exact Phase 5 stub pattern from 05-01-SUMMARY).

    (a) `src/app/display/page.tsx` — `"use client"`, `export default function DisplayPage()`:
    - `const containerRef = useRef<HTMLDivElement>(null);`
    - `const [isFullscreen, setIsFullscreen] = useState(false);`
    - `const [countdown, setCountdown] = useState<number | null>(null);`
    - Call `const { state, status, participantCount } = useGameSync(GAME_ID, HOST_SENTINEL_PLAYER_ID, { onEvent: (event) => { if (event.type === "COUNTDOWN_STARTED") setCountdown(event.seconds); } });` — use HOST_SENTINEL_PLAYER_ID, NEVER null (Finding 1).
    - `useEffect` adding a `fullscreenchange` document listener that sets `isFullscreen(document.fullscreenElement !== null)`; cleanup removes it.
    - `useEffect([countdown])` driving the countdown interval: `if (countdown === null || countdown <= 0) return; const t = setInterval(() => setCountdown((c) => (c !== null && c > 1 ? c - 1 : null)), 1000); return () => clearInterval(t);` (functional updater — Pitfall 3).
    - Phase switch identical in structure to GuestShell: if `!state` render `<LoadingDisplay />`; else switch on `state.phase` → lobby `<LobbyDisplay participantCount={participantCount} />`, question `<QuestionDisplay state={state} />`, locked `<LockedDisplay state={state} />`, revealed `<RevealDisplay state={state} />`, ended `<WinnerDisplay state={state} />`, default uses `const _exhaustive: never = state.phase; void _exhaustive;` then `<LoadingDisplay />`.
    - Root container: `<div ref={containerRef} className="relative min-h-dvh w-screen overflow-hidden bg-ink">` containing, in order: `<DisplayStatusDot status={status} />`; the fullscreen button rendered only when `!isFullscreen`; `{countdown !== null && <CountdownOverlay countdown={countdown} />}`; then the phase screen. The countdown overlay must render ALONGSIDE the screen (z-layered), NEVER as an early-return replacement (Pitfall 7).
    - Fullscreen button: `<button type="button" onClick={() => containerRef.current?.requestFullscreen()} className="fixed top-4 right-4 z-50 glass rounded-lg px-4 py-2 text-[1.5vw] font-body text-champagne-dim hover:text-champagne transition-opacity duration-300" aria-label="Activează ecran complet">Ecran complet</button>`. Must be a direct click handler (not inside useEffect) — Pitfall 4.

    (b) `src/components/display/LoadingDisplay.tsx` — `export function LoadingDisplay()`: full-screen centered column per UI-SPEC §LoadingDisplay — CSS spinner `<div className="w-[6vw] h-[6vw] rounded-full border-4 border-gold/30 border-t-gold animate-spin" />` + `<p className="text-[1.5vw] font-normal font-body text-champagne-dim">Se încarcă...</p>`. Container `flex min-h-dvh flex-col items-center justify-center gap-[3vh] bg-ink`.

    (c) `src/components/display/LobbyDisplay.tsx` — `export function LobbyDisplay({ participantCount }: { participantCount: number })` (D-01): full-screen centered column. Title `<h1 className="text-[6vw] font-bold font-heading text-champagne text-center leading-tight">Joc — Cristina & Andrei</h1>`, `.thin-divider`, count block with the number span at `text-[4vw] font-bold font-heading text-champagne` and a one-shot pulse: `const [pulsing, setPulsing] = useState(false); useEffect(() => { setPulsing(true); const t = setTimeout(() => setPulsing(false), 600); return () => clearTimeout(t); }, [participantCount]);` apply `cn("text-[4vw] font-bold font-heading text-champagne", pulsing && "animate-pulse")`. Label span `text-[1.5vw] font-normal font-body text-champagne-dim` reading `{participantCount} jucători s-au alăturat` (interpolate the count into the label string). Subtitle `<p className="text-[1.5vw] font-normal font-body text-champagne-dim/60 mt-[2vh]">Așteptăm să înceapă...</p>`. `"use client"` at top (uses hooks).

    (d) `src/components/display/DisplayStatusDot.tsx` — `export function DisplayStatusDot({ status }: { status: SyncStatus })` (D-11): import `SyncStatus` type from `@/hooks/useGameSync` and `cn` from `@/lib/utils`. Fixed container `className="fixed top-[calc(3.5rem+0.5rem)] right-4 z-50 flex items-center gap-[0.5vw]"` with `role="status" aria-live="polite"`. Dot div `cn("w-[0.8vw] h-[0.8vw] min-w-[8px] min-h-[8px] rounded-full", status === "connected" && "bg-sage", status === "connecting" && "bg-gold", status === "reconnecting" && "bg-gold animate-pulse", status === "error" && "bg-red-500")`. Label map: connected → null (no text), connecting → "Se conectează...", reconnecting → "Reconectare...", error → "Eroare"; render the label span only when label is non-null with classes per UI-SPEC §DisplayStatusDot (connecting text-champagne-dim, reconnecting text-gold, error text-red-400). `"use client"`.

    (e) Five compiling STUBS (replaced by 06-02 / 06-03) — each `"use client"`, each accepts the prop the shell passes and renders a minimal placeholder so the build passes. Use the REAL prop signatures so 06-02/06-03 drop in without changing page.tsx:
    - `src/components/display/QuestionDisplay.tsx` — `export function QuestionDisplay({ state }: { state: GameStateSnapshot })` → render `<div className="flex min-h-dvh items-center justify-center bg-ink"><p className="text-[2vw] text-champagne-dim">{state.currentQuestion?.body ?? ""}</p></div>` (note: `.body`, not `.text`).
    - `src/components/display/LockedDisplay.tsx` — same prop signature, minimal placeholder.
    - `src/components/display/RevealDisplay.tsx` — same prop signature, minimal placeholder.
    - `src/components/display/WinnerDisplay.tsx` — `export function WinnerDisplay({ state }: { state: GameStateSnapshot })`, minimal placeholder reading `state.leaderboard`.
    - `src/components/display/CountdownOverlay.tsx` — `export function CountdownOverlay({ countdown }: { countdown: number })`, minimal placeholder `<div className="fixed inset-0 z-40 flex items-center justify-center bg-ink/80"><span>{countdown}</span></div>`.
    Import `GameStateSnapshot` type from `@/hooks/useGameSync` in the stubs that need it.

    Do NOT add a `src/app/display/layout.tsx`. The root layout already provides `<html lang="ro">` and fonts. The display page is a single client component.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/display/page.tsx` exists and exports a default `DisplayPage`
    - DisplayPage calls `useGameSync(GAME_ID, HOST_SENTINEL_PLAYER_ID, ...)` — NOT null (grep confirms no `useGameSync(GAME_ID, null` in the file)
    - The phase switch references LobbyDisplay, QuestionDisplay, LockedDisplay, RevealDisplay, WinnerDisplay, LoadingDisplay
    - `<DisplayStatusDot status={status} />` and the "Ecran complet" button are rendered in the root container
    - The countdown overlay is rendered conditionally alongside the phase screen (not via early return)
    - All eight `src/components/display/*.tsx` files exist
    - LobbyDisplay reads `participantCount` and pulses the count on change
    - DisplayStatusDot renders bg-sage when connected, bg-gold animate-pulse when reconnecting
    - No display component imports `canvas-confetti`
    - Stub components use `state.currentQuestion?.body` (not `.text`) where they read the question
    - `npm run build` exits 0 and the build output lists the `/display` route
  </acceptance_criteria>
  <done>`/display` route compiles and renders: LoadingDisplay before sync, LobbyDisplay with live pulsing count in lobby phase, persistent status dot, and a fullscreen button. The five stub screens compile with their real prop signatures, ready for 06-02/06-03 to replace.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — live lobby, sync, and fullscreen</name>
  <action>Pause for human verification. The executor has completed all automated work (Tasks 1-2); this checkpoint verifies the live lobby, real-time sync, and fullscreen behaviour in a browser before the phase proceeds. No code is written in this task — follow the steps below and wait for the resume signal.</action>
  <what-built>
    The `/display` route: a full-screen landscape lobby that auto-syncs with the live game channel, a persistent connection status dot (green/amber), and an "Ecran complet" fullscreen button. The Wave-0 infra (keyframes, useGameSync onEvent callback, POST /api/host/countdown) is in place but not yet visually wired (countdown overlay and game screens land in 06-02/06-03).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. In one tab, open `http://localhost:3000/host`, enter the host password, and confirm the game is in lobby (use the Control tab; if the game is mid-round, click "Joc Nou / Reseteaza Jocul" in the Emergency panel to return to lobby).
    3. In a second tab (or on a laptop attached to a TV), open `http://localhost:3000/display`.
    4. Confirm: the display shows the large "Joc — Cristina & Andrei" title and a participant count, all on a dark full-screen canvas; text is large and readable.
    5. Confirm the connection status dot is visible top-right — green (no label) when connected.
    6. Open `http://localhost:3000/` on a phone or third tab and join with a name. Within ~1-2 seconds the display's participant count should increase and briefly pulse — WITHOUT refreshing the display tab (DISP-02).
    7. Click "Ecran complet" on the display — it should enter browser fullscreen and the button should disappear. Press Esc to exit.
    8. (Optional resilience) Toggle network off/on in the display tab's DevTools; the status dot should turn amber (pulsing) then return to green.
  </how-to-verify>
  <resume-signal>Type "approved" if the lobby renders, syncs the live count, and fullscreen works — or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host client → POST /api/host/countdown | Untrusted request body crosses into the server; host password header must be validated |
| Supabase Broadcast → display client | Display re-fetches authoritative state; never trusts broadcast payload for game data |
| public browser → /display route | Display route is intentionally public (read-only subscriber); no sensitive data served |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | POST /api/host/countdown | mitigate | `validateHostAuth(req)` is the first statement; unauthenticated requests rejected 401 before any broadcast |
| T-06-02 | Tampering | countdown `seconds` body field | mitigate | Reject non-integer or out-of-range (1-10) seconds with 400 — prevents a 999s overlay DoS on the TV |
| T-06-03 | Information Disclosure | /display public route | accept | Intentional per architecture (D-10) — display shows only already-public game state; no player PII beyond display names already shown on guest leaderboard |
| T-06-04 | Tampering | leaderboard / question text rendering | mitigate | React renders all strings as text nodes; no `dangerouslySetInnerHTML` anywhere in display components (XSS-safe) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in Phase 6 (RESEARCH §Package Legitimacy Audit: not applicable) — no install step, no supply-chain surface |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run lint` exits 0
- Build output includes the `/display` route and `/api/host/countdown` route
- `grep -n "HOST_SENTINEL_PLAYER_ID" src/app/display/page.tsx` returns a match; `grep -n "useGameSync(GAME_ID, null" src/app/display/page.tsx` returns nothing
- Manual: `/display` renders the lobby, syncs the participant count live, fullscreen button works, status dot reflects connection state (checkpoint Task 3)
</verification>

<success_criteria>
- DISP-01: `/display` renders a full-screen landscape layout with large viewport-unit typography, functioning independently of the host dashboard
- DISP-02: The display auto-syncs with host actions (participant count updates live) and shows a persistent connection status dot
- Wave-0 infra complete: keyframes in globals.css, useGameSync onEvent callback (backward compatible), POST /api/host/countdown with host-auth + 1-10 seconds clamp
- All eight display component files exist (3 real, 5 compiling stubs) with correct prop signatures for 06-02/06-03
- Build and lint green
</success_criteria>

<output>
Create `.planning/phases/06-tv-display-mode/06-01-SUMMARY.md` when done.
</output>
