---
phase: 07-polish-pre-event-hardening
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/display/LobbyDisplay.tsx
  - src/components/display/RevealDisplay.tsx
  - src/components/display/WinnerDisplay.tsx
autonomous: true
requirements: [RT-07]
must_haves:
  truths:
    - "The TV lobby title and TV winner #1 name render in a gold-to-champagne gradient"
    - "On the TV reveal screen the correct option label renders in the gold gradient; the leaderboard rows stagger in (top-to-bottom, ~80ms apart) once per reveal, not on every score tick"
    - "On the TV winner screen the full leaderboard staggers in once and a gold/champagne/blush confetti burst fires once from upper-center"
    - "Devices with prefers-reduced-motion see plain (non-staggered) leaderboard lists, no confetti, and flat gold gradient text"
  artifacts:
    - path: "src/components/display/LobbyDisplay.tsx"
      provides: "Gold gradient game title"
      contains: "text-gradient-gold"
    - path: "src/components/display/RevealDisplay.tsx"
      provides: "Gradient correct label + staggered top-5 leaderboard"
      contains: "motion.ol"
    - path: "src/components/display/WinnerDisplay.tsx"
      provides: "Gradient #1 name + staggered full leaderboard + TV confetti"
      contains: "canvas-confetti"
  key_links:
    - from: "src/components/display/WinnerDisplay.tsx"
      to: "canvas-confetti"
      via: "dynamic import in useEffect (ref-guarded)"
      pattern: "import\\(\"canvas-confetti\"\\)"
    - from: "src/components/display/RevealDisplay.tsx"
      to: "motion/react"
      via: "motion.ol/motion.li stagger + useReducedMotion"
      pattern: "from \"motion/react\""
---

<objective>
Upgrade the three TV display screens that carry the room's most cinematic moments to the soft-luxury wedding aesthetic: gold-gradient hero text, staggered "building-to-#1" leaderboard rows on reveal and winner screens, and a room-wide confetti burst on the winner screen.

This is a vertical slice on the TV surface: after this plan, a person watching the projector sees gold-gradient titles, a drum-roll leaderboard stagger on every reveal, and a celebratory confetti moment at game end. Per D-01 these motion upgrades stay on the TV surface only.

Purpose: RT-07 (soft-luxury aesthetic) — TV display gradient text, leaderboard stagger (D-03), TV winner confetti (D-09).
Output: `.text-gradient-gold` applied to LobbyDisplay title, WinnerDisplay #1 name, and RevealDisplay correct label; inline staggered leaderboards in RevealDisplay (top-5) and WinnerDisplay (full); canvas-confetti on WinnerDisplay mount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-pre-event-hardening/07-CONTEXT.md
@.planning/phases/07-polish-pre-event-hardening/07-RESEARCH.md
@.planning/phases/07-polish-pre-event-hardening/07-UI-SPEC.md
@.planning/phases/07-polish-pre-event-hardening/07-PATTERNS.md
@.planning/phases/07-polish-pre-event-hardening/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gradient on LobbyDisplay title + RevealDisplay gradient/stagger</name>
  <files>src/components/display/LobbyDisplay.tsx, src/components/display/RevealDisplay.tsx</files>
  <read_first>
    - src/components/display/LobbyDisplay.tsx — confirm the game-title `<h1>` (around line 39) currently uses `text-champagne`. Confirm the pulse mechanism, participant count block, and waiting subtitle (which must stay unchanged).
    - src/components/display/RevealDisplay.tsx — confirm the `OptionWithBar` correct-label `<span>` (around lines 74-79) uses `isCorrect ? "text-gold-bright" : "text-champagne"`; confirm the `LeaderboardPanel` block (around lines 165-173) wrapped in `transform scale-150 origin-top`; confirm the existing `LeaderboardPanel` import.
    - src/components/guest/LeaderboardPanel.tsx — read the row structure and the `getRankClasses(rank)` / `getScoreClasses(rank)` helper functions (around lines 21-30, 17-69) to copy them inline. DO NOT import them from there.
    - .planning/phases/07-polish-pre-event-hardening/07-PATTERNS.md §"src/components/display/RevealDisplay.tsx" — exact replacement block, module-level variants, helpers, and the scale-150 wrapper preservation.
    - .planning/phases/07-polish-pre-event-hardening/07-UI-SPEC.md §"Animation 2" — stagger variant spec.
  </read_first>
  <action>
    LobbyDisplay.tsx: on the game-title `<h1>`, replace `text-champagne` with `text-gradient-gold`. No other change; no new imports needed.

    RevealDisplay.tsx:
    - Add imports `import { motion, useReducedMotion } from "motion/react";` and `import { Separator } from "@/components/ui/separator";`. Remove the now-unused `LeaderboardPanel` import (Critical Note 2 — unused import will fail lint).
    - Add module-level helper functions `getRankClasses(rank)` (rank 1 → `"text-gold-bright font-bold"`, rank ≤3 → `"text-champagne"`, else `"text-champagne-dim"`) and `getScoreClasses(rank)` (rank 1 → `"text-gold-bright font-bold"`, else `"text-gold"`) copied from LeaderboardPanel — these are LOCAL, do not import.
    - Add module-level `containerVariants` (`hidden: {}`, `visible: { transition: { staggerChildren: 0.08, delayChildren: 0.1 } }`) and `rowVariants` (`hidden: { opacity: 0, y: 10 }`, `visible: { opacity: 1, y: 0, transition: { duration: 0.25, ease: "easeOut" } }`).
    - In the component body add `const shouldReduce = useReducedMotion();`.
    - In `OptionWithBar`, change the correct-label className branch from `isCorrect ? "text-gold-bright"` to `isCorrect ? "text-gradient-gold"` (the `: "text-champagne"` branch stays).
    - Replace the `<LeaderboardPanel leaderboard={state.leaderboard.slice(0, 5)} />` call with an inline staggered list. PRESERVE the surrounding `<div className="transform scale-150 origin-top">` and `thin-divider` wrapper (required for TV readability — Phase 6 Finding 5). Render a "Clasament" heading, then: when `shouldReduce`, a plain `<ol>`/`<li>` list; otherwise a `motion.ol` (variants=containerVariants, initial="hidden", animate="visible") with `motion.li` rows (variants=rowVariants). Use `state.leaderboard.slice(0, 5)`. Each row shows `#{rank}`, the name (with `getRankClasses`), and `{entry.score} pt` (with `getScoreClasses`), with a `<Separator className="bg-champagne/10" />` between rows (not after the last). Wrap the whole block in `state.leaderboard.length > 0 && (...)`.

    Stagger must run once on mount only — do NOT add `state.leaderboard` to any effect/initial deps (Pitfall 5). The AnimatePresence key in display/page.tsx (plan 01) remounts this component on phase change; within the `revealed` phase, score ticks must NOT re-trigger the stagger. Animate only `opacity` and `y` (transform) — never layout properties.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `LobbyDisplay.tsx` game-title h1 className contains `text-gradient-gold` and no longer contains `text-champagne` on that element
    - `RevealDisplay.tsx` no longer imports `LeaderboardPanel`
    - `RevealDisplay.tsx` contains `motion.ol` and `motion.li`
    - `RevealDisplay.tsx` OptionWithBar correct branch uses `text-gradient-gold` (was `text-gold-bright`)
    - `RevealDisplay.tsx` still contains `transform scale-150 origin-top` (scale wrapper preserved)
    - `RevealDisplay.tsx` contains `staggerChildren: 0.08` and `useReducedMotion`
    - `RevealDisplay.tsx` defines local `getRankClasses` and `getScoreClasses` (not imported)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>LobbyDisplay title and RevealDisplay correct label use the gold gradient; RevealDisplay shows a staggered top-5 leaderboard (with reduced-motion plain fallback) inside the preserved scale-150 wrapper; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: WinnerDisplay gradient + staggered full leaderboard + TV confetti (D-09)</name>
  <files>src/components/display/WinnerDisplay.tsx</files>
  <read_first>
    - src/components/display/WinnerDisplay.tsx — confirm current imports (Trophy, GameStateSnapshot type, LeaderboardPanel), the winner-name `<p>` (around line 40) using `text-gold-bright`, the `LeaderboardPanel` block (around lines 55-57) inside `transform scale-150 origin-top`, and the JSDoc line stating "No canvas-confetti — confetti is guest-side only (Phase 5)".
    - src/components/guest/WinnerScreen.tsx — read the confetti `useEffect` (lines 17-49): the `confettiFired` useRef guard, the dynamic `import("canvas-confetti").then(({ default: confetti }) => {...})`, and the empty deps array. Replicate this structure with the TV (D-09) parameters.
    - src/components/guest/LeaderboardPanel.tsx — row structure + `getRankClasses`/`getScoreClasses` (to copy inline, NOT import).
    - .planning/phases/07-polish-pre-event-hardening/07-PATTERNS.md §"src/components/display/WinnerDisplay.tsx" — exact import block, confetti effect, gradient swap, and leaderboard replacement (full leaderboard, no slice).
    - .planning/phases/07-polish-pre-event-hardening/07-UI-SPEC.md §"Animation 5" — TV confetti params and origin.
  </read_first>
  <action>
    Update imports: add `import { useEffect, useRef } from "react";`, `import { motion, useReducedMotion } from "motion/react";`, and `import { Separator } from "@/components/ui/separator";`. Remove the `LeaderboardPanel` import (replaced by inline stagger; unused import fails lint).

    In the component body add `const confettiFired = useRef(false);` and `const shouldReduce = useReducedMotion();`. Add a `useEffect` with empty deps `[]` that fires the TV confetti exactly once on mount: guard `if (shouldReduce) return;` then `if (confettiFired.current) return;` then set `confettiFired.current = true;`, then `import("canvas-confetti").then(({ default: confetti }) => confetti({ particleCount: 150, spread: 100, origin: { x: 0.5, y: 0.3 }, colors: ["#f0c060", "#f5e6c8", "#d4a843", "#e8a0a0"] }));`. This is the room-wide celebration burst (D-09); origin `y: 0.3` (upper-center) makes confetti fall down across the landscape projector. Use the DYNAMIC import only — never a static top-level `import confetti from "canvas-confetti"` (Pitfall 4, keeps it out of the initial bundle).

    Winner name `<p>`: replace `text-gold-bright` with `text-gradient-gold`.

    Replace the `<LeaderboardPanel leaderboard={state.leaderboard} />` call with the SAME inline staggered list pattern as RevealDisplay (module-level `containerVariants`/`rowVariants`/`getRankClasses`/`getScoreClasses`, reduced-motion plain `<ol>` fallback, `motion.ol`/`motion.li` otherwise, `<Separator>` between rows). Use the FULL `state.leaderboard` (NO `.slice(0, 5)` — the winner screen shows everyone). PRESERVE the `transform scale-150 origin-top` wrapper.

    Update the JSDoc: remove the "No canvas-confetti — confetti is guest-side only (Phase 5)" line and note that D-09 adds the TV winner confetti.

    Animate only `opacity` and `y`. Stagger runs once on mount (no `state.leaderboard` in deps — Pitfall 5).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `WinnerDisplay.tsx` no longer imports `LeaderboardPanel`
    - `WinnerDisplay.tsx` contains `import("canvas-confetti")` (dynamic, not static top-level)
    - `WinnerDisplay.tsx` confetti effect uses `particleCount: 150`, `spread: 100`, `origin: { x: 0.5, y: 0.3 }`
    - `WinnerDisplay.tsx` confetti effect guards on `confettiFired.current` and `shouldReduce`
    - `WinnerDisplay.tsx` winner name `<p>` uses `text-gradient-gold` (was `text-gold-bright`)
    - `WinnerDisplay.tsx` contains `motion.ol`/`motion.li` and `staggerChildren: 0.08`, using full `state.leaderboard` (no `.slice(0, 5)`)
    - `WinnerDisplay.tsx` still contains `transform scale-150 origin-top`
    - JSDoc no longer says "No canvas-confetti"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>WinnerDisplay shows a gold-gradient #1 name, a staggered full leaderboard (reduced-motion plain fallback), and a one-shot 150-particle confetti burst from upper-center; canvas-confetti is dynamically imported and ref-guarded; build passes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created or first-introduced by THIS plan (exclude from drift verification):
- Local module-level helpers `getRankClasses(rank)` and `getScoreClasses(rank)` newly defined inside `RevealDisplay.tsx` and `WinnerDisplay.tsx` (copies of LeaderboardPanel helpers — intentionally local, not imports)
- Module-level `containerVariants` and `rowVariants` constants in `RevealDisplay.tsx` and `WinnerDisplay.tsx`
- `shouldReduce` const (from `useReducedMotion()`) in `RevealDisplay.tsx` and `WinnerDisplay.tsx`
- `confettiFired` useRef and the TV confetti `useEffect` newly added to `WinnerDisplay.tsx`
- New `motion`, `useReducedMotion`, `Separator`, `useEffect`, `useRef` imports added to these display components
- Application of the `.text-gradient-gold` class (utility itself created in plan 01)

Pre-existing symbols this plan consumes (NOT new): `.text-gradient-gold` (created in plan 01), `state.leaderboard`, `state.phase`, `GameStateSnapshot`, `Trophy`, `OptionWithBar`, the `transform scale-150 origin-top` wrappers, `thin-divider`, `canvas-confetti` (already installed), `Separator` component, and the LeaderboardPanel helper logic (copied, not referenced).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

This plan introduces NO new trust boundaries. Evidence:
- No new API routes (modifies three client display components only)
- No new user input (all data comes from the existing `state` prop fed by `useGameSync`)
- Player display names continue to render as React text nodes — no `dangerouslySetInnerHTML` (existing Phase 5/6 pattern maintained)
- No new client-side Supabase queries; service-role key remains server-only

| Boundary | Status |
|----------|--------|
| client → API | unchanged — no new routes or fetches |
| untrusted player name → DOM | unchanged — React text node escaping preserved (no innerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering (XSS) | Leaderboard row rendering (player names) | accept | Names rendered as React text nodes (`{entry.name}`), auto-escaped; no `dangerouslySetInnerHTML` introduced (RESEARCH.md §Security Domain) |
| T-07-04 | Denial of Service | canvas-confetti burst on WinnerDisplay | accept | One-shot, ref-guarded, fires once per game-end; dynamic import keeps it off initial bundle; runs locally on the TV browser only |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | NO new packages — `motion` and `canvas-confetti` already installed and audited in prior phases (RESEARCH.md §Package Legitimacy Audit). No install task in this plan. |

ASVS L1 conclusion: no new attack surface (no routes, no auth changes, no new user input). All threats accepted with documented rationale.
</threat_model>

<verification>
- `npm run build` exits 0 after each task.
- Manual (deferred to plan 04 perf/dry-run gate): on `npm run dev`, advancing to the reveal phase shows the top-5 leaderboard staggering in once; ending the game shows the full leaderboard stagger + upper-center confetti; gradient text visible on lobby title, winner name, and reveal correct label.
- Reduced-motion: with OS reduce-motion enabled, leaderboards render as plain lists, no confetti fires, gradient text is flat gold.
</verification>

<success_criteria>
- LobbyDisplay title, WinnerDisplay #1 name, and RevealDisplay correct option label render in the gold gradient.
- RevealDisplay (top-5) and WinnerDisplay (full) leaderboards stagger in once per mount, inside the preserved `scale-150` wrapper, with a reduced-motion plain-list fallback.
- WinnerDisplay fires a one-shot, ref-guarded, dynamically-imported 150-particle confetti burst from upper-center, skipped under reduced motion.
- No `LeaderboardPanel` import remains in either display component; helpers are local.
- `npm run build` exits 0.
</success_criteria>

<output>
Create `.planning/phases/07-polish-pre-event-hardening/07-02-SUMMARY.md` when done.
</output>
