---
phase: 07-polish-pre-event-hardening
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/guest/QuestionScreen.tsx
  - src/components/guest/NameGate.tsx
  - src/components/guest/RevealScreen.tsx
autonomous: true
requirements: [RT-07]
must_haves:
  truths:
    - "Tapping the A or B answer button on a guest phone briefly scales it to 0.96 (tactile press feedback), then returns"
    - "The guest name-entry heading renders in the gold-to-champagne gradient"
    - "A guest who answered correctly sees a small gold/champagne confetti burst once when the reveal arrives; a guest who answered wrong sees no confetti"
    - "Devices with prefers-reduced-motion get no tap-scale animation, no confetti, and flat gold gradient text"
  artifacts:
    - path: "src/components/guest/QuestionScreen.tsx"
      provides: "motion.button A/B with whileTap scale 0.96"
      contains: "whileTap"
    - path: "src/components/guest/NameGate.tsx"
      provides: "Gold gradient name-entry heading"
      contains: "text-gradient-gold"
    - path: "src/components/guest/RevealScreen.tsx"
      provides: "Correct-answer confetti mini-burst (derived correctness)"
      contains: "canvas-confetti"
  key_links:
    - from: "src/components/guest/RevealScreen.tsx"
      to: "canvas-confetti"
      via: "dynamic import in useEffect, ref-guarded, derived correctness"
      pattern: "import\\(\"canvas-confetti\"\\)"
    - from: "src/components/guest/QuestionScreen.tsx"
      to: "motion/react"
      via: "motion.button whileTap + useReducedMotion"
      pattern: "from \"motion/react\""
---

<objective>
Add the guest-surface polish that makes the phone experience feel responsive and celebratory: a tactile tap-scale on the A/B answer buttons, a gold-gradient heading on the join screen, and a small confetti burst when a guest gets the answer right at reveal.

This is a vertical slice on the guest surface: after this plan, a guest tapping an answer feels the button press, sees a premium gold-gradient title on join, and is rewarded with confetti when they answer correctly. Per D-01/D-04 the guest app stays CSS-only with NO AnimatePresence and NO phase-transition animation — only `whileTap` and a one-shot confetti — to protect reliability on low-end Android.

CRITICAL: `GameStateSnapshot` has NO `playerAnsweredCorrectly` field. The correct-answer confetti trigger MUST be derived as `state.myAnswer !== null && state.myAnswer === state.correctOption`. Do not reference `playerAnsweredCorrectly` (it does not exist — TypeScript strict mode will error; confetti would never fire).

Purpose: RT-07 (soft-luxury aesthetic) — guest tap feedback (D-04), gradient heading (D-06), correct-answer confetti (D-08).
Output: motion.button with whileTap on QuestionScreen A/B; `.text-gradient-gold` on NameGate h1; ref-guarded dynamic-import confetti on RevealScreen for correct answers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-pre-event-hardening/07-CONTEXT.md
@.planning/phases/07-polish-pre-event-hardening/07-RESEARCH.md
@.planning/phases/07-polish-pre-event-hardening/07-UI-SPEC.md
@.planning/phases/07-polish-pre-event-hardening/07-PATTERNS.md
@.planning/phases/07-polish-pre-event-hardening/07-01-SUMMARY.md
@src/hooks/useGameSync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guest A/B tap feedback (motion.button whileTap) + NameGate gradient heading</name>
  <files>src/components/guest/QuestionScreen.tsx, src/components/guest/NameGate.tsx</files>
  <read_first>
    - src/components/guest/QuestionScreen.tsx — read the two `<button>` elements (A around lines 136-155, B around lines 158-177) and the `getButtonClass()` function (around lines 40-72). Note that `getButtonClass`'s return string already includes `[touch-action:manipulation]` and `focus-visible:ring-2 focus-visible:ring-gold/50` — these MUST carry over unchanged. Note the existing `useState` calls and `handleTap` async function.
    - src/components/guest/NameGate.tsx — confirm the `<h1>` (around line 78) currently uses `text-champagne`. Confirm the form, input, error state, and submit button (which stay unchanged).
    - .planning/phases/07-polish-pre-event-hardening/07-PATTERNS.md §"src/components/guest/QuestionScreen.tsx" and §"src/components/guest/NameGate.tsx" — exact open-tag replacements and import additions.
    - .planning/phases/07-polish-pre-event-hardening/07-UI-SPEC.md §"Animation 3" — whileTap spec and preservation checklist.
  </read_first>
  <action>
    QuestionScreen.tsx:
    - Add `import { motion, useReducedMotion } from "motion/react";`.
    - In the component body (after the existing `useState` calls) add `const shouldReduce = useReducedMotion();`.
    - Convert BOTH the A and B `<button type="button" ...>` elements to `<motion.button type="button" ...>` and change both closing `</button>` tags to `</motion.button>`.
    - On each, add `whileTap={shouldReduce ? undefined : { scale: 0.96 }}` and `transition={{ duration: 0.1, ease: "easeOut" }}`.
    - PRESERVE on each: `className={getButtonClass("A" | "B", localAnswer, state.phase)}` exactly (this carries `[touch-action:manipulation]` — required so iOS does NOT reintroduce the 300ms tap delay, Pitfall 3), `onClick={() => handleTap("A" | "B")}`, `aria-pressed`, `aria-disabled`, `aria-label`, and all inner `<span>` children. Do NOT modify `getButtonClass` or `handleTap`.
    - whileTap animates `scale` (transform) only — no layout properties; this is required to hold 60fps on the 4x CPU throttle gate (plan 04).

    NameGate.tsx:
    - On the `<h1>` heading, replace `text-champagne` with `text-gradient-gold`. No other change; no new imports needed.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `QuestionScreen.tsx` imports `motion` and `useReducedMotion` from `"motion/react"`
    - `QuestionScreen.tsx` contains two `motion.button` elements (A and B), each with `whileTap={shouldReduce ? undefined : { scale: 0.96 }}`
    - `QuestionScreen.tsx` no longer contains bare `<button type="button"` for the A/B options (converted to motion.button)
    - `QuestionScreen.tsx` still passes `className={getButtonClass(...)}` to each motion.button (touch-action + focus-visible preserved via class string)
    - `getButtonClass` and `handleTap` function bodies are unchanged
    - `NameGate.tsx` h1 className contains `text-gradient-gold` and no longer `text-champagne` on that element
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>A/B answer buttons are motion.button with `whileTap` scale 0.96 (reduced-motion → no scale), preserving touch-action/focus/aria; NameGate heading uses the gold gradient; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Correct-answer confetti on RevealScreen (derived correctness, D-08)</name>
  <files>src/components/guest/RevealScreen.tsx</files>
  <read_first>
    - src/components/guest/RevealScreen.tsx — read the full file. Confirm current imports (cn, CheckCircle2/XCircle, GameStateSnapshot/SyncStatus types, SyncStatusBadge, LeaderboardPanel) and that it already derives correctness using `state.correctOption` and `state.myAnswer` for its reveal UI. Confirm there is currently NO confetti. The existing `getRevealClass`, distribution bar, and `LeaderboardPanel` call must stay unchanged.
    - src/hooks/useGameSync.ts (lines 53-66) — confirm `GameStateSnapshot` has `myAnswer: "A"|"B"|null` and `correctOption: "A"|"B"|null` and has NO `playerAnsweredCorrectly` field.
    - src/components/guest/WinnerScreen.tsx (lines 17-49) — the confetti `useEffect` reference pattern: `confettiFired` useRef, dynamic `import("canvas-confetti").then(({ default: confetti }) => {...})`, empty deps. Replicate with D-08 mini-burst params.
    - .planning/phases/07-polish-pre-event-hardening/07-PATTERNS.md §"src/components/guest/RevealScreen.tsx" — exact import additions and confetti effect with the CRITICAL derived-correctness condition.
    - .planning/phases/07-polish-pre-event-hardening/07-UI-SPEC.md §"Animation 4" — particle params.
  </read_first>
  <action>
    Add imports `import { useEffect, useRef } from "react";` (merge with any existing react import if present) and `import { useReducedMotion } from "motion/react";`.

    In the component body add `const confettiFired = useRef(false);` and `const shouldReduce = useReducedMotion();`.

    Add a `useEffect` with empty deps `[]` that fires the correct-answer mini-burst exactly once on mount. Inside the effect, derive correctness as `const answeredCorrectly = state.myAnswer !== null && state.myAnswer === state.correctOption;` — this is the CRITICAL derivation (RESEARCH.md §CRITICAL FINDING: `playerAnsweredCorrectly` does NOT exist on `GameStateSnapshot`). Then guard in order: `if (!answeredCorrectly) return;`, `if (shouldReduce) return;`, `if (confettiFired.current) return;`, then `confettiFired.current = true;`, then `import("canvas-confetti").then(({ default: confetti }) => confetti({ particleCount: 60, spread: 50, origin: { y: 0.6 }, colors: ["#f0c060", "#f5e6c8", "#d4a843"] }));`.

    Use the DYNAMIC import only — never a static top-level `import confetti from "canvas-confetti"` (Pitfall 4). Empty deps with the condition checked INSIDE preserves the single-fire ref-guard contract (Pitfall 5); `state` is captured via closure. A guest who answered wrong (or did not answer) sees no confetti; a guest who answered correctly sees exactly one burst.

    Do NOT modify `getRevealClass`, the CheckCircle2/XCircle indicators, the distribution bar, or the `LeaderboardPanel` call. Do NOT stagger the guest leaderboard (guest surface keeps the original LeaderboardPanel — D-01 reliability priority). Confetti is a one-shot imperative call (no CSS animation, so no globals.css change).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `RevealScreen.tsx` contains `import("canvas-confetti")` (dynamic, not static top-level)
    - The confetti effect derives correctness as `state.myAnswer !== null && state.myAnswer === state.correctOption`
    - `RevealScreen.tsx` does NOT contain the string `playerAnsweredCorrectly`
    - The confetti effect uses `particleCount: 60`, `spread: 50`, `origin: { y: 0.6 }`
    - The effect guards on `answeredCorrectly`, `shouldReduce`, and `confettiFired.current` before firing
    - The effect has an empty deps array `[]`
    - `getRevealClass`, distribution bar, and the `LeaderboardPanel` call are unchanged
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>RevealScreen fires a one-shot 60-particle gold/champagne confetti burst when (and only when) the guest's recorded answer equals the correct option, using a dynamic import + ref-guard + reduced-motion skip; correctness is derived (no `playerAnsweredCorrectly`); build passes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created or first-introduced by THIS plan (exclude from drift verification):
- `shouldReduce` const (from `useReducedMotion()`) newly added to `QuestionScreen.tsx` and `RevealScreen.tsx`
- `confettiFired` useRef and the correct-answer confetti `useEffect` newly added to `RevealScreen.tsx`
- Conversion of the A/B `<button>` elements to `motion.button` with `whileTap` in `QuestionScreen.tsx`
- New `motion`, `useReducedMotion`, `useEffect`, `useRef` imports added to these guest components
- Application of the `.text-gradient-gold` class to NameGate (utility itself created in plan 01)

Pre-existing symbols this plan consumes (NOT new): `.text-gradient-gold` (created in plan 01), `getButtonClass`, `handleTap`, `localAnswer`, `state.phase`, `state.myAnswer`, `state.correctOption`, `GameStateSnapshot`, `getRevealClass`, `LeaderboardPanel`, `SyncStatusBadge`, `CheckCircle2`/`XCircle`, `canvas-confetti` (already installed).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

This plan introduces NO new trust boundaries. Evidence:
- No new API routes (modifies three client guest components only)
- No new user input handling beyond what already exists — the NameGate form, input, and submit are unchanged; only the heading class changes
- The A/B tap path is unchanged logically (`onClick`/`handleTap` preserved); only a visual `whileTap` is added
- Player/answer data comes from the existing `state` prop; no new client-side Supabase queries

| Boundary | Status |
|----------|--------|
| client → API (answer submit) | unchanged — `handleTap` and `onClick` preserved verbatim |
| guest name input → DB | unchanged — NameGate form logic untouched (only h1 class changed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | QuestionScreen A/B `motion.button` | accept | Logic (`onClick`/`handleTap`/aria/`getButtonClass`) preserved verbatim; only a visual `whileTap` transform added; no new input path |
| T-07-06 | Denial of Service | canvas-confetti on RevealScreen | accept | One-shot, ref-guarded, fires only on correct answer; dynamic import keeps it off initial bundle; runs locally on the guest phone |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | NO new packages — `motion` and `canvas-confetti` already installed and audited in prior phases (RESEARCH.md §Package Legitimacy Audit). No install task in this plan. |

ASVS L1 conclusion: no new attack surface (no routes, no auth changes, no new user input). All threats accepted with documented rationale.
</threat_model>

<verification>
- `npm run build` exits 0 after each task.
- Manual / device (deferred to plan 04 perf/dry-run gate): on a phone, tapping A/B shows the brief 0.96 scale; the join heading shows the gold gradient; answering correctly then advancing to reveal shows the mini confetti burst; answering wrong shows none.
- Reduced-motion: with OS reduce-motion enabled, no tap-scale, no confetti, flat gold heading.
</verification>

<success_criteria>
- A/B answer buttons are `motion.button` with `whileTap` scale 0.96 (reduced-motion → none), preserving touch-action, focus-visible, aria, onClick.
- NameGate heading uses the gold gradient.
- RevealScreen fires a one-shot 60-particle confetti burst only when `state.myAnswer === state.correctOption` (and `myAnswer !== null`), via dynamic import + ref-guard + reduced-motion skip.
- No reference to `playerAnsweredCorrectly` anywhere.
- `npm run build` exits 0.
</success_criteria>

<output>
Create `.planning/phases/07-polish-pre-event-hardening/07-03-SUMMARY.md` when done.
</output>
